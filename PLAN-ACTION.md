# La ville est belle — Jeu de campagne · Plan d'action (handoff)

> Document autonome destiné à être exécuté par un autre modèle, session par session.
> Objectif produit : **donner envie d'aller écouter le morceau "La ville est belle" de PMC.**
> Cible : **navigateur mobile** (Safari iOS en priorité absolue, puis Chrome & Firefox iOS/Android). Portrait natif.
> Deadline publique : **11 septembre 2026**. Développement par sessions courtes.

---

## État d'avancement (mis à jour en session)

**Fait — Étapes 0 à 3 :**
- ✅ **Étape 0 — Fondations.** `config.js`, `index.html` (safe-area, viewport), Vite (`server.host:true`, LAN par défaut), boucle à pas fixe 120 Hz + interpolation au rendu, mode debug (touche `d`).
- ✅ **Étape 1 — Route nue.** Moteur pseudo-3D en scanlines (`road.js`), défilement, déport latéral, grille rythmique de debug.
- ✅ **Étape 2 — Audio & synchro.** Web Audio (`audio.js`), unlock iOS sur geste ("Touche l'écran pour jouer"), `clock.js` basculé sur `AudioContext.currentTime` au démarrage, **fondu d'entrée/sortie** + **volume réglable** (slider vertical bas-droit) ajoutés en plus du brief initial. *(Le pulse visuel au tempo a été essayé puis retiré — flash blanc trop gênant à l'usage ; à retenter plus tard avec un traitement plus doux.)*
- ✅ **Étape 3 — Le cycliste.** 3 silhouettes proposées et testées à 32 px (artifact), **silhouette B "penché en avant" choisie et intégrée** (`player.js`, pixel art 26×34 pré-rendu puis blit à l'échelle). Gyroscope (`deviceorientation`, calibré à la prise en main, permission iOS sur le geste de démarrage) avec repli tactile automatique si refusé/indisponible. Bouton saut tactile (noir) + barre d'espace (hop visuel seulement, pas encore d'obstacle). Courbe de réponse pilotée par `agressiviteVirages`, léger lean du sprite dans les virages.

**Fait — Étape 4 (Paris) :**
- ✅ Caméra remontée/reculée deux fois (retour utilisateur : encore trop près du sol la 1ère fois) — `CAMERA_HEIGHT` 1.1→2.4→4.8, `HORIZON_RATIO` 0.42→0.33→0.30.
- ✅ Ciel en dégradé coucher de soleil (violet profond → mauve → orange chaud à l'horizon).
- ✅ **Façades haussmanniennes détaillées** (`world.js`, remplace les blocs plats) — d'après plusieurs références envoyées par l'artiste (pixel art + rendus 3D "rue de Paris"). Par bâtiment : pierre crème/ocre (4 teintes, variation aléatoire déterministe), toit mansardé vert-de-gris avec faîtage, corniche claire, cheminées (si assez large à l'écran), grille de fenêtres (sombres + ~7% éclairées en jaune chaud, effet "quartier habité au soleil couchant"). Détail désactivé sous un seuil de taille écran (bâtiments trop petits/lointains) pour rester lisible et léger.
- ✅ **Sol teinté par la lumière chaude** (`road.js`) — chaussée, trottoirs, bords et ligne centrale se fondent progressivement vers la couleur de l'horizon (`HAZE_COLOR`) à mesure qu'ils s'éloignent, exactement comme les bâtiments : les deux partagent désormais les mêmes constantes de brume (`HAZE_COLOR`/`HAZE_MAX_Z`/`HAZE_STRENGTH`, exportées par `road.js`, importées par `world.js`) pour une atmosphère cohérente sur toute la scène, et `HAZE_COLOR` reprend exactement la couleur du dernier stop du ciel pour que l'horizon se fonde vraiment dedans. Dégradés vers la brume précalculés une seule fois au chargement du module (pas à chaque ligne/frame) pour ne pas alourdir la boucle de rendu du sol (jusqu'à 160 bandes/frame).
- ✅ Testé en preview mobile (375×812, dark) : **120 fps stable**, aucun coût perf mesurable malgré les façades détaillées + le fondu du sol.
- ⬜ Reste : vérif 60/120 fps sur iPhone réel (seul point encore non validé sur vrai appareil pour cette étape).

**Fait — Étape 5 (Gameplay) :**
- ✅ `entities.js` — spawn de bonus/obstacles calé sur les beats (même technique que la grille de debug : formule pure par index de créneau, pas de tableau d'instances). 5 bonus, 3 obstacles (portière/piéton à éviter en se déportant, trottinette à sauter — le saut ignore la position latérale, comme prévu au brief).
- ✅ 3 vies, énergie (drain continu, +bonus, ralentissement à 0 plutôt que game over), score — testés en preview (collision obstacle → vie perdue confirmé, ramassage bonus → score+50 confirmé).
- ✅ HUD minimal en haut à droite (score, vies, barre d'énergie) — bare, le vrai habillage vient à l'étape 6.
- ✅ Fin de partie : game over (0 vie) ou fin de morceau, écran bare avec score final. La route/les entités se figent, l'audio continue de jouer jusqu'au bout (cohérent avec l'objectif "donner envie d'écouter le morceau" — mourir ne coupe pas la musique).
- 🐛 **Bug trouvé et corrigé** : au premier test réel, game over en ~7s (3 vies perdues quasi instantanément) — trop d'obstacles trop tôt, donnait l'impression que "rien ne bouge" (écran figé sur Game Over dès le début). Corrigé dans `entities.js` : proportion bonus/obstacle 55%→75%, + 8s de grâce sans obstacle en début de course. Re-testé : 30+s sans dégât même sans aucune action du joueur.
- ⬜ Pas encore testé sur iPhone réel (gyro + perf). ✅ Bouton "Rejouer" ajouté depuis (voir plus bas) — plus besoin de recharger la page après un game over/fin de course.
- ✅ **Fluidité du personnage** (`main.js`) : vélocité latérale lissée (accélère/décélère au lieu de suivre l'input instantanément), lean dérivé de la vélocité réelle, léger rebond de pédalage (cadence proportionnelle à la vitesse).
- ✅ **Animation de pédalage** (demandé explicitement : le perso ne bougeait pas assez, "on dirait une trottinette électrique") — `player.js` pré-rend maintenant **4 frames** (jambe gauche haute → transition → jambe droite haute → transition, au lieu d'un sprite figé + simple rebond vertical), sélectionnées selon la même phase que le rebond (`pedalPhase`, `main.js`). `PEDAL_RATE` recalé à ~0,125 s par frame à la vitesse de départ (11 u/s, 2× plus rapide que le premier calage) — pédale plus vite quand la vitesse monte, comme un vrai vélo.
- ✅ **Roue agrandie + cheveux plus bouclés** (retouches demandées sur le même sprite) — `player.js` : roue 5.5→8 px de rayon (avant, quasi entièrement cachée sous le short/les jambes, lisait comme une trottinette à petite roue) ; cheveux passés de 4 bosses concentrées en haut à 8 bosses réparties sur tout le pourtour (silhouette "nuage" bouclé au lieu d'un contour presque lisse). **Limite atteinte du dessin procédural** (rectangles/cercles en Canvas) — voir §11 : liste d'assets créa à produire pour remplacer ça proprement par du vrai pixel art.
- ✅ **Entités en pixel art** (`entities.js`) : les bonus/obstacles étaient dessinés en vectoriel avec des contours (`stroke`) trop visibles, incohérents avec le sprite du joueur. Repris en icônes pré-rendues basse résolution (comme `player.js`), plus de bords épais. Le piéton était lu comme un "poteau" — silhouette élargie pour mieux se reconnaître.

**Relecture de code complète (demandée explicitement) — 3 bugs trouvés et corrigés :**
- 🐛 **Slider volume → fuite vers la direction.** Glisser le doigt sur le slider volume remontait jusqu'aux listeners `window` de `input.js` et dirigeait aussi le personnage (repli tactile), faute de `stopPropagation`. Corrigé — même traitement que le bouton saut. Vérifié : `x` du joueur reste immobile pendant qu'on règle le volume.
- 🐛 **Lean du sprite non plafonné.** `playerState.lean` pouvait dépasser `MAX_LEAN` si `sensibiliteDirection` (config) est réglé au-dessus de 1 — clamp explicite ajouté dans `main.js`.
- 🧹 Commentaires en-tête obsolètes (`road.js`, `clock.js`, `debug.js` référençaient "étape 2/3/5" au futur alors que fait) + reset d'état canvas manquant dans `debug.js` — nettoyés.

**Bug remonté par playtest réel (artiste) — 1 bug trouvé et corrigé (2 itérations) :**
- 🐛 **Les bonus/obstacles disparaissaient pile à hauteur du joueur.** Signalé comme « je choppe tous les objets quoi que je fasse » — la fausse piste initiale était la taille des hitbox (`HIT_RADIUS`/`PICKUP_RADIUS`), qui en réalité n'a rien d'anormal. Le vrai bug : dans `entities.js`, `render()` arrêtait de dessiner un objet dès que `resolved` le marquait (touché, ramassé, **ou raté**), c'est-à-dire dès qu'il franchissait la fenêtre de collision autour de `PLAYER_NEAR_Z` — l'objet s'évaporait donc en plein milieu de la route au lieu de continuer sa trajectoire jusqu'au bas de l'écran comme le reste du décor.
  - 1ère itération : `render()` ignorait `resolved` entièrement → plus rien ne disparaissait, y compris les objets vraiment touchés/ramassés (mauvais aussi : un objet ramassé doit disparaître net à l'impact, ça faisait déjà bien son effet pour la clémentine).
  - 2e itération (comportement final) : nouveau Set `consumed`, sous-ensemble de `resolved` — rempli uniquement quand il y a vraiment eu événement (bonus ramassé ou obstacle touché), jamais pour un simple raté. `render()` saute uniquement les objets `consumed`. Résultat : touché/ramassé → disparaît net à l'impact (score/vie mis à jour au même moment) ; raté → continue sa trajectoire jusqu'à sortir du champ de vision comme n'importe quel élément du décor.
  - Vérifié en preview : ramassage/collision confirmés cohérents pour les 5 bonus et 3 obstacles (même chemin de code, pas de traitement spécial par type) ; objets ratés vus continuer jusqu'aux bords de l'écran sur plusieurs séquences.

**Bug remonté par playtest réel (artiste) — les objets pas encore arrivés « sautaient » légèrement au ramassage d'un bonus :**
- 🐛 **Cause** : les bonus/obstacles n'ont pas de position propre stockée — `entities.js` recalcule chaque frame leur profondeur (`z`) à partir de « temps restant avant leur temps musical × vitesse **courante** ». Ramasser un bonus augmente `game.energy` instantanément (`main.js`), qui pilote `speedMultiplier`, qui pilotait jusque-là `currentSpeed` **sans lissage** (`road.js`) — donc `currentSpeed` changeait d'un coup dans la même frame, et tous les objets pas encore arrivés se retrouvaient recalculés à une profondeur légèrement différente au même instant (décalage visible, surtout après plusieurs ramassages rapprochés).
- ✅ **Corrigé** dans `road.js` : `currentSpeed` rattrape maintenant sa cible progressivement (`SPEED_SMOOTHING`, même principe que le lissage de vélocité du joueur dans `main.js`) au lieu de la recopier telle quelle chaque frame. Le changement de vitesse devient une transition sur ~1/3 s au lieu d'un saut d'une frame à l'autre — n'affecte pas la montée en vitesse globale du morceau (déjà progressive sur 257 s, le lissage y est imperceptible).
- Vérifié en preview : partie rejouée après le correctif, score et défilement cohérents, aucune régression de vitesse/rythme observée. Le décalage étant subtil par nature, une confirmation visuelle définitive reste à faire par l'artiste sur téléphone réel.

**Bouton "Rejouer" après fin de partie (demandé explicitement, priorisé avant l'étape 6) :**
- ✅ Ajouté un bouton DOM `#replay-button` (`index.html`, caché par défaut, affiché via classe `.visible`), affiché sur les deux écrans de fin (game over **et** parcours terminé — pas de raison de réserver ça au seul game over).
- ✅ `audio.js` : nouvelle fonction `restart()` (stoppe la source en cours, relance une lecture depuis 0 — le contexte audio reste débloqué, pas besoin d'un nouveau gesture iOS).
- ✅ `road.js` : nouvelle fonction `reset()` (remet `distanceScrolled`/vitesse à zéro).
- ✅ `entities.js` : `reset()` vide maintenant `resolved` **et** `consumed`.
- ✅ `main.js` : `restartGame()` centralise la remise à zéro complète (joueur, saut, vies/énergie/score, route, entités) + relance l'horloge sur l'audio redémarré. Vérifié en preview : cycle complet fin de morceau → clic Rejouer → partie repart de zéro (score/vies/distance/temps tous à zéro, bouton disparaît).

**Placement des boutons optimisé pour iPhone en navigateur (demandé explicitement) :**
- 🎯 **Problème visé** : `env(safe-area-inset-bottom)` ne couvre que le home indicator matériel — il ne compte **pas** la barre d'onglets/adresse de Safari iOS, qui reste affichée par-dessus le bas de l'écran tant que la page ne scrolle pas (ce qui est le cas ici, `overflow:hidden`). Sans correctif, `#jump-button`/`#volume-control` collés au bord bas via la seule safe-area risquaient de finir partiellement masqués/durs à taper sous cette barre sur un vrai iPhone.
- ✅ **Correctif dynamique** (`main.js`) : nouvelle variable CSS `--browser-chrome-bottom`, mise à jour en temps réel via `window.visualViewport` (`resize`/`scroll`) — elle mesure la vraie hauteur mangée par le chrome du navigateur (`innerHeight - visualViewport.height - visualViewport.offsetTop`) et s'ajoute à la marge basse des deux boutons dans `index.html`. S'adapte tout seul, y compris si Safari montre/cache sa barre en cours de partie.
- ✅ Marges statiques resserrées par la même occasion : `#jump-button` 16px→20px (bas **et** gauche — la gauche aussi pour rester à distance du geste de retour au bord de l'écran iOS), `#volume-control` 12px→16px bas / 12px→14px droite.
- Vérifié en preview : aucune régression visuelle/fonctionnelle, `--browser-chrome-bottom` se calcule bien à `0px` dans cet environnement (pas de chrome Safari à simuler ici — **la vraie validation de ce correctif ne peut se faire que sur un iPhone réel**, ça rejoint la priorité n°1 déjà en attente).

**4 retours de playtest réel traités dans la foulée (demandés explicitement) :**
- 🐛 **Perte de vie alors qu'on n'est pas sur la trajectoire de l'obstacle.** Signalé : « je perds des points alors que je ne suis pas sur le trou dans la chaussée ». Cause : dans `entities.js`, la trottinette ignorait complètement la position latérale du joueur (`if (!isJumping)` seul décidait) — un vestige du choix initial « seul le saut sauve » (§5). Corrigé : la trottinette applique maintenant le même test `HIT_RADIUS` que les autres obstacles, **en plus** du saut — dans sa voie, se déporter OU sauter fonctionne tous les deux ; hors de sa voie, plus aucun risque. Vérifié en preview (voir plus bas, capture avec anneau rouge sur l'obstacle touché uniquement quand aligné).
- ✅ **Indicateur de direction à l'écran** (« un giro comme sur n'importe quel jeu mobile, pour comprendre »). `main.js` : nouveau `renderGyroIndicator()` — pastille en haut à gauche, piste horizontale + curseur qui suit l'axe combiné `getSteer()` (donc représentatif quelle que soit la source active : giro sur mobile, tactile en repli, clavier en dev), curseur orange quand l'inclinaison dépasse 70 % de l'intensité max. Vérifié pixel par pixel via `getImageData` (le curseur est passé de `#f5e6c8` centré à `#ff7a4d` en butée droite en pressant → relâchant Flèche droite) — **la preview navigateur affiche par-dessus un gros curseur natif orange en haut à gauche qui n'a rien à voir avec le jeu** (confirmé absent du canvas via `getImageData`, voir pièges ci-dessous) ; ne pas confondre les deux au prochain test.
- ✅ **Plus d'objets + meilleure différenciation bonus/obstacle.** `config.js` : `cadenceSpawnBeats` 2 → 1.5 (un événement toutes les 0,75 s au lieu d'1 s). `entities.js` : chaque icône (bonus **et** obstacle) a maintenant un anneau de couleur superposé au dessin — vert (`BONUS_RING`) pour les 5 bonus, rouge (`OBSTACLE_RING`) pour les 3 obstacles — signal universel indépendant de la forme/couleur propre à chaque objet. Vérifié en preview : anneaux verts/rouges nettement visibles sur tous les types rencontrés.
- ✅ **Fond derrière le HUD** (« on n'arrive pas à percevoir » score/vies/énergie). `main.js` : `renderHud()` dessine désormais un panneau arrondi translucide (`rgba(11,10,20,0.55)`) derrière tout le cluster avant le texte/les vies/la barre — nouveau helper `roundRect()`. Vérifié en preview : lisible même sur les zones claires du ciel.

**Session d'audit — nettoyage ciblé (demandé explicitement, hors numérotation des étapes) :**
- ✅ `CLAUDE.md` : les 2 lignes de méthode qui pointaient encore vers "commence à l'Étape 0 + Étape 1" (périmées depuis longtemps) renvoient maintenant vers l'État d'avancement de ce fichier plutôt que de figer une étape.
- ✅ `KICKOFF.md` supprimé — orphelin, jamais référencé, même défaut que ci-dessus (donnait la même instruction périmée).
- 🐛 **Bug corrigé** dans `world.js` : la couleur de façade de chaque bâtiment (`mix(shape.facade, HAZE_COLOR, hazeT)`) recevait une chaîne `"rgb(...)"` là où le parsing attendait du hex `"#RRGGBB"` — échouait silencieusement, et la façade de **tous** les bâtiments se peignait avec la couleur de la corniche au lieu de sa propre teinte (jamais remarqué : les deux tons sont proches). Corrigé en même temps qu'un chantier perf : dégradés vers la brume précalculés au chargement du module pour les façades/toits/fenêtres, comme `road.js` le fait déjà pour le sol (`buildHazeGradient`/`gradientStep`/`hexToRgb` exportés depuis `road.js`, réutilisés par `world.js` au lieu d'une deuxième implémentation). Les façades varient maintenant vraiment d'un bâtiment à l'autre.
- ✅ **`hud.js` créé** : `renderStartPrompt`/`renderHud`/`renderGyroIndicator`/`renderEndScreen` extraits de `main.js` (qui les gardait tous en dur, voir plus haut) vers ce nouveau module, appelé depuis `main.js` sur le même principe que `road.js`/`world.js`/`player.js`/`entities.js`. `main.js` 457→318 lignes.
- ✅ **Mode debug étendu** (`debug.js`/`clock.js`/`entities.js`/`main.js`) : raccourcis actifs uniquement si le mode debug est allumé — `↑` avance l'horloge de jeu de 10s, `F` force la fin du morceau, `G` force un game over, `B`/`O` forcent l'apparition d'un bonus/obstacle pile devant le joueur (même chemin de code que le spawn normal). Rappel affiché dans l'overlay FPS. But : ne plus attendre les 257,9s réelles à chaque test d'écran de fin. `clock.jumpBy()` n'affecte que l'horloge de jeu, pas la lecture audio elle-même (qui continue depuis sa position réelle) — décalage assumé, c'est un outil de dev.
- ✅ **Git initialisé** (demandé explicitement, plusieurs sessions plus tard — voir `AUDIT.md` à la racine pour l'audit qui avait motivé la question à l'origine). `git init` + `.gitignore` (`node_modules/`, `dist/`, `.netlify/`, `.DS_Store`, `.claude/settings.local.json`, et `reels/` — dossier sans rapport avec le jeu, présent incidemment dans ce répertoire). Commit initial sur `main` (tout l'état du projet à ce jour, jamais suivi avant), puis branche de travail `gameplay-pause-voitures-etoiles` créée pour la session en cours — `main` reste le dernier état "connu bon"/déployé tant que cette branche n'est pas mergée.

**Fait — Étape 6 (Habillage & concours) :**
- ✅ **Écran-titre.** Le titre "LA VILLE EST BELLE" a été ajouté à l'écran de démarrage existant (`hud.js`, `renderStartPrompt`) plutôt que d'introduire un nouvel état séparé — la machine à états reste à 3 écrans (démarrage/jeu/fin), pas 4.
- ✅ **CTA "aller écouter".** Vrai lien `<a href>` DOM (`#cta-link`, `index.html`) vers `CONFIG.lienEP`, sur le même principe que `#replay-button` — pas de hit-test sur le canvas. Visible sur l'écran de démarrage et sur les deux écrans de fin, caché pendant la partie (`main.js` : `tryStartGesture`/`endGame`/`restartGame` pilotent la classe `.visible`, même cycle que `#replay-button`). Même traitement `stopPropagation` (pointerdown + touch) que les autres contrôles superposés au canvas, pour ne pas déclencher le geste de démarrage en tapant dessus.
- ✅ **Logique dates concours** (`hud.js`, `contestStatus()`). Compare `Date.now()` à `CONFIG.dateOuverture`/`dateFermeture`, affiché sous le score sur l'écran de fin ("Concours dès le [date]" / "Score valable pour le concours" / "Concours terminé"). Le jeu reste jouable dans tous les cas, rien n'est bloqué — seul ce texte change. Pas de backend pour l'instant (étape 7) donc rien n'est réellement conditionné encore ; l'étape 7 pourra réutiliser le même calcul pour décider si un score envoyé compte pour de vrai.
- Vérifié en preview : écran-titre lisible sans déborder (375px de large), CTA cliquable avec le bon `href` (`https://linktr.ee/pmc.mp3`, confirmé via l'inspection de page), caché pendant la partie, réapparaît sur les deux écrans de fin avec le statut concours correct (aujourd'hui : "Concours dès le 11 septembre", cohérent avec la date d'ouverture). Aucune erreur console.
- ⬜ Pas testé sur iPhone réel (comme tout le reste — voir §10).

**🐛 Premier playtest sur iPhone réel — bug bloquant trouvé et corrigé (audio + horloge) :**
- **Symptômes remontés** (identiques sur Safari ET Chrome iOS, y compris en navigation privée — donc *pas* un problème de cache, contrairement à la première intuition) : aucun son, les immeubles défilent normalement mais **les bonus/obstacles restent figés sur place**, impossible de marquer le moindre point, et la partie ne se termine jamais.
- **Cause unique aux trois symptômes.** Le décor et les entités n'utilisent pas la même source de temps : `road.js` avance sa distance avec `dt` (temps réel de frame), alors que `entities.js` positionne **tout** à partir du temps musical `clock.now()`. Or `clock` était branchée sur `audio.now()` = `audioCtx.currentTime - startCtxTime`. **Un AudioContext qui ne tourne pas a un `currentTime` qui n'avance pas** → horloge de jeu collée à 0 → entités figées, score impossible, condition de fin (`now >= dureeMorceau`) jamais atteinte — pendant que le décor, lui, continuait de défiler. D'où l'impression trompeuse que « le jeu tourne mais les objets sont cassés ».
- **Pourquoi le contexte ne tournait pas** : `audio.js` créait l'`AudioContext` **dans le `.then()` du fetch**, donc hors geste utilisateur — iOS le laisse alors `suspended`. Et `unlockAndStart()` appelait `resume()` **sans l'attendre** puis démarrait la lecture immédiatement, en lisant `startCtxTime` sur une horloge encore gelée. Invisible en preview desktop (Chrome reprend instantanément et n'exige pas de contexte créé sur geste) — d'où 6 étapes sans que ça se voie.
- ✅ **Correctif 1 — unlock iOS conforme** (`audio.js`, réécrit) : décodage fait au chargement via un **`OfflineAudioContext`** (aucune sortie audio, donc aucune restriction iOS, et le morceau est prêt avant le tap) ; l'`AudioContext` de sortie est créé **dans le handler du geste** ; un **buffer muet** y est démarré synchroniquement (c'est lui qui débloque réellement la sortie sur iOS, `resume()` seul ne suffit pas toujours) ; la lecture n'est armée qu'une fois `state === "running"`, pour que `startCtxTime` soit lu sur une horloge qui avance. Retombée de secours : si le décodage hors-ligne échoue, une 2e tentative a lieu avec le contexte de sortie (copie de l'ArrayBuffer gardée, `decodeAudioData` détachant celui qu'on lui passe).
- ✅ **Correctif 2 — la partie ne peut plus se figer** (`main.js`, `clock.js`) : c'est le vrai correctif de fond. La bascule sur l'horloge audio se fait sur `audio.isRunning()` (et non plus `hasStarted()`, qui était vrai même sur un contexte suspendu). Si l'audio ne tourne pas **3 s après le tap**, la partie démarre quand même sur `performance.now()` ; et un chien de garde surveille en continu l'horloge audio pendant la course (appel entrant, mise en arrière-plan iOS…) : si elle se fige plus d'**1 s**, on repasse en secours **sans perdre la progression** (`clock.setTimeSource(fn, preserve)`, nouveau 2e paramètre). Principe retenu : *un jeu muet reste jouable, un jeu figé non*. Un bandeau « Son indisponible — recharge la page » (`hud.renderAudioWarning`) le signale, pour que le silence ne soit pas pris pour le comportement normal — l'objectif du projet étant justement d'amener au morceau.
- ✅ **Correctif 3 — mode debug utilisable sur téléphone** (`debug.js`) : il n'était activable qu'au clavier (`CONFIG.toucheDebug`), donc **inaccessible depuis un iPhone**, ce qui est précisément là où les bugs apparaissent. Ajout de l'activation par URL : **`?debug`** (ex. `http://192.168.1.29:5173/?debug`). Deux nouvelles lignes dans l'overlay, `audio=` (état du contexte) et `horloge=` (`audio` ou `secours`), qui permettent de diagnostiquer à distance sans console.
- **Vérifié en preview** : (a) cas nominal — `audio=lecture`, `horloge=audio`, temps qui avance, entités qui défilent, score qui monte ; (b) cas dégradé — en remplaçant `AudioContext` par un stub bloqué en `suspended` (simulation exacte du bug iOS, possible seulement parce que le contexte est désormais créé sur le geste), la partie démarre bien au bout de 3 s sur l'horloge de secours, `horloge=secours`, score et défilement normaux, bandeau affiché. Aucune erreur console.
- ⬜ **Reste à confirmer sur l'iPhone de l'artiste** : le correctif vise une contrainte iOS que la preview desktop ne reproduit pas — la validation ne peut se faire qu'en vrai. Rappel : sur iOS, le Web Audio est **coupé par le bouton silencieux physique** ; si le son manque toujours alors que `?debug` affiche `audio=lecture` et `horloge=audio`, c'est là qu'il faut chercher.

**2e playtest iPhone — audio confirmé OK + 5 retours traités :**
- ✅ **Audio validé sur appareil réel.** `audio=lecture` / `horloge=audio` confirmés par l'artiste sur son iPhone : le correctif de la session précédente tient. Le silence résiduel venait bien du **bouton silencieux physique** (le Web Audio y est soumis sur iOS) — vérifié dans les deux positions par l'artiste.
- ✅ **Objets +20 %** (`entities.js`) : `ICON_WORLD` 0.85 → 1.02. Volontairement **sans toucher aux hitbox** (`PICKUP_RADIUS` 1.3 / `HIT_RADIUS` 0.9), déjà validées au playtest précédent : le rayon de ramassage reste supérieur à l'icône, donc pas de décalage visuel/collision introduit.
- ✅ **Déplacement latéral plus rapide** : `sensibiliteDirection` 1.0 → 1.25 dans `config.js` (le seul fichier de réglages — pas de constante moteur touchée).
- ✅ **Halo au ramassage d'un bonus** (`player.js`, `renderPickupGlow` + `pickupFlash` dans `main.js`) : demandé « très, très léger ». Dégradé radial vert (même teinte que l'anneau des bonus), **composite `lighter`** donc additif, opacité max 0,35, durée 0,35 s, qui grossit légèrement en s'estompant pour lire comme une impulsion. Dessiné **sous** le sprite pour ne pas délaver le personnage. Purement cosmétique, aucune mécanique touchée. Vérifié deux fois : en isolé (`import()` du module + `getImageData` : intensité 0 → rgb(0,0,0), intensité 1 → rgb(38,76,41) sur fond noir) et en jeu (verdeur autour du joueur : base 75 = le maillot, pic 118 au ramassage).
- ✅ **Contrôles remontés au-dessus de la barre Safari** (`index.html`, `main.js`). Retour : « le slider est en bas mais il faut trouver un moyen de l'afficher à l'écran ». **Cause** : sur iOS, `window.innerHeight` exclut **déjà** la barre d'outils de Safari, donc `--browser-chrome-bottom` (mesuré via `visualViewport`, ajouté la session précédente) y vaut ~0 et ne rattrapait rien — les contrôles se retrouvaient derrière la barre, invisibles. **Correctif** : nouvelle variable CSS `--controls-bottom` = `max(64px, …)` — un vrai **plancher** de 64px, la mesure dynamique restant en complément là où le chrome recouvre vraiment la page. Appliquée à `#jump-button` **et** `#volume-control`. Bloc volume raccourci 190→150px (piste ~131px, curseur tourné 130→118px pour ne pas déborder) et inset dynamique plafonné à 140px côté JS (mesure aberrante = clavier virtuel/zoom pincé). Vérifié : les deux contrôles à exactement 64px du bord bas, niveau de volume lisible d'un coup d'œil (remplissage orange).
- ⬜ **Gyroscope : toujours pas validé.** C'est la demande principale de l'artiste (« je veux pencher le téléphone, pas appuyer sur l'écran ») et elle est **impossible à satisfaire en HTTP** : iOS exige un contexte sécurisé pour `DeviceOrientationEvent`. Un tunnel `cloudflared` a été monté pour ce test (voir ci-dessous). Le code gyro (`input.js`) n'a pas été modifié — il n'a jamais pu s'exécuter, donc rien ne prouve encore qu'il soit correct : à retester en priorité en HTTPS, en regardant la ligne `direction=` de l'overlay `?debug` (doit passer de `tactile/clavier` à `gyroscope`).

**3e playtest iPhone — gyroscope : aucune popup de permission (en cours) :**
- **Symptôme** : en HTTPS (tunnel cloudflared), l'artiste n'obtient **aucune boîte de dialogue** d'autorisation. Pas un refus visible : rien du tout. La direction reste au glissement tactile.
- **Le code gyro lui-même est correct** — vérifié cette session en simulant les réponses d'iOS (stub de `DeviceOrientationEvent.requestPermission` + événements `deviceorientation` synthétiques) : `denied` → enregistré ; promesse rejetée → `erreur : NotAllowedError` ; `granted` → écoute posée, `gamma` lu, `gyroActive` vrai, `getSteer()` à **+0,72 penché à droite / −0,72 à gauche**. Donc dès que la permission passe, le pilotage à l'inclinaison fonctionne. Le problème est **uniquement** l'obtention de la permission.
- **Hypothèse principale** : iOS renvoie `denied` **sans afficher de popup** quand *Réglages → Safari → Accès au mouvement et à l'orientation* est désactivé. C'est le seul cas qui produit exactement « il ne se passe rien ».
- **Hypothèse secondaire** : `requestPermission()` était appelé depuis le `pointerdown` global qui déclenche aussi le déblocage audio — l'activation transitoire peut être consommée entre-temps, et iOS refuse alors silencieusement.
- ✅ **Correctifs apportés** (les deux hypothèses sont couvertes) :
  - **Bouton dédié `#tilt-button` « Activer l'inclinaison »** sur l'écran de démarrage (`index.html`, `main.js`), avec un vrai handler `click` isolé — plus de partage de geste avec l'audio. Affiché **uniquement** là où la plateforme réclame une permission (`DeviceOrientationEvent.requestPermission` existe), donc invisible sur desktop/Android.
  - **État affiché à l'écran** (`#tilt-status`, rafraîchi toutes les 0,5 s) : permission renvoyée, nombre d'événements reçus, alerte si la page n'est pas en contexte sécurisé, et le rappel du réglage iOS quand la réponse est `denied`. Sans ça, un refus iOS est indiscernable d'un bug — il n'y a ni console ni clavier sur un téléphone.
  - **Diagnostic dans `input.js`** (`getGyroDiag()`) : permission, compteur d'événements, dernier `gamma`, `isSecureContext`. Repris aussi dans l'overlay `?debug` (ligne `gyro:`) pour l'avoir en cours de partie.
  - Garde-fou explicite : en HTTP, on n'appelle même plus `requestPermission()` (rejet garanti) — on enregistre `refusé : page non sécurisée (HTTP)`.
- ✅ **Bouton SAUT masqué** (`index.html`, à la demande : « enlève-le pour l'instant, je veux que les gens tournent »). La **mécanique** de saut reste intacte dans le code (barre d'espace, `consumeJumpPress`) : rien ne devient injouable, la trottinette s'évitant aussi en se déportant depuis le correctif de playtest précédent. Réafficher = retirer le `display: none` sur `#jump-button`.
- ⬜ **Toujours à confirmer sur l'iPhone** : ce que raconte la ligne d'état sous le bouton. C'est elle qui tranchera entre réglage iOS désactivé, refus mémorisé, et absence d'événements malgré l'accord.

**🐛 Écran noir sur iPhone — bug introduit par le bloc gyro, trouvé et corrigé :**
- **Symptôme** : « il n'y a plus rien, il n'y a pas d'image » + statut lu juste avant : `Permission : erreur : NotAllowedError · événements reçus : 0`.
- **Cause 1 — plantage iOS uniquement.** Dans `main.js`, le bloc d'activation gyro appelait `ctaLink.classList.add("pushed")` alors que `const ctaLink` était déclaré **plus bas** dans le fichier → **zone morte temporelle → `ReferenceError` qui interrompt tout le module**, donc aucune boucle de rendu : écran noir. Le bloc ne s'exécutant que si `DeviceOrientationEvent.requestPermission` existe, **le plantage ne pouvait se produire que sur iOS** — jamais reproductible en preview desktop, où la condition est fausse. ✅ Corrigé en déplaçant le bloc après la déclaration de `ctaLink`, avec un commentaire d'avertissement sur place.
- **Cause 2 — `NotAllowedError`.** `#tilt-button` n'avait pas de garde sur **`pointerdown`** (seulement `touchstart`/`touchend`), or c'est `pointerdown` qu'écoute le geste de démarrage — et il précède `click`. Taper le bouton lançait donc la partie, et `tryStartGesture` appelait `requestGyroPermission()` **avant** le handler du bouton : iOS n'accordant qu'une seule activation transitoire par geste, le premier appel la consommait et le second repartait en `NotAllowedError`. ✅ Corrigé sur deux fronts : gardes `pointerdown`/`pointerup` ajoutées sur le bouton, et `tryStartGesture` ne demande **plus du tout** la permission quand la plateforme en exige une (`if (!needsTiltPermission)`) — sur iOS, seul le bouton dédié la demande.
- **Vérifié** en simulant la plateforme iOS (stub de `requestPermission` injecté **avant** le chargement du module, via un script temporaire retiré depuis — indispensable car `needsTiltPermission` est figé à l'évaluation du module) : au chargement, bouton affiché et statut `pas encore demandée`, **plus aucun plantage** ; un tap sur le bouton → **exactement 1** appel à `requestPermission`, la partie ne démarre pas, statut mis à jour ; réponse `granted` → bouton « Inclinaison activée » en vert, `getSteer()` réagit au `gamma`, puis un tap ailleurs démarre la partie et le canvas rend bien (pixel central non noir).

**Refonte de la direction artistique + courbure de la planète (gyroscope validé par l'artiste au passage) :**
- ✅ **Gyroscope confirmé fonctionnel sur iPhone** (« OK, ça marche »). Le pilotage à l'inclinaison est donc acquis ; le bouton SAUT reste masqué.
- ✅ **Police de la charte** : `StageGrotesk` fournie par l'artiste (`~/Downloads/3. DA - Sous-Titres ►/Font 01 (StageGrotesk) ►/`). Deux graisses seulement copiées dans `public/fonts/` — **Black (900)** pour titres/score/boutons, **Medium (500)** pour le texte courant — soit 148 ko ; les 7 graisses auraient coûté ~520 ko sur data mobile. Déclarées en `@font-face` dans `index.html`, exposées via la variable CSS `--police`. ⚠️ **Le canvas ne lit pas les variables CSS** : `hud.js` redéfinit les mêmes valeurs en constantes de module (`POLICE`/`BLANC`/`ROUGE`/`PANNEAU`) — toute modif de charte doit être reportée aux deux endroits. Et comme un `ctx.font` demandé avant chargement retombe silencieusement sur la police système, `main.js` force `document.fonts.load()` des deux graisses avant la première frame.
- ✅ **Palette d'après la pochette de l'EP** (photo fournie : béton brut, ciel bleu franc, mur rouge vif). Rouge de charte **`#E13E26`**, confirmé par le dossier `ROUGE - #E13E26` du même répertoire DA. Appliqué partout : ciel bleu profond → rouge à l'horizon (`road.js`), chaussée bitume quasi noir + marquages **blanc pur**, trottoirs béton chaud, façades gris clair minéral, toits/fenêtres quasi noirs, fenêtres éclairées orange-rouge (`world.js`), HUD blanc/noir avec le rouge en unique accent (`hud.js`, `index.html`).
- ✅ **Anneaux des objets repassés en blanc (bonus) / rouge (obstacles)** au lieu de vert/rouge — le vert n'existe pas dans la charte, et sur bitume noir le blanc est ce qui ressort le plus. Le halo de ramassage (`player.js`) suit en blanc. ⚠️ À signaler à l'artiste : « le truc vert » qu'il attrapait est désormais blanc.
- ✅ **Courbure de la planète** (`road.js`), demandée : « la piste est plate, j'aimerais sentir que la Terre est ronde, que l'horizon disparaisse, qu'on voie sur 10 bâtiments ». Modèle : le sol s'enfonce de `CURVATURE·z²` (approximation parabolique de la sphère). La caméra surplombe donc de plus en plus le sol lointain, et **la route cesse de converger vers un point** : elle bascule derrière la courbe. Il en découle une distance maximale visible, `HORIZON_Z = √(CAMERA_HEIGHT/CURVATURE) ≈ 95` unités — soit une dizaine de bâtiments à `SPACING = 10`, exactement la demande.
  - Intégré **uniquement dans `project()`**, la fonction que traversent déjà `world.js`, `player.js`, `entities.js` et `debug.js` : toute la scène épouse la courbe sans qu'aucun de ces modules ait à la connaître.
  - Le rendu du sol doit inverser la projection (y → z) : `c·z² − Y·z + h = 0`, résolu sous la forme `2h/(Y+√Δ)` plutôt que `(Y−√Δ)/2c` — identique mathématiquement, mais sans l'annulation catastrophique près de l'horizon.
  - **Culling obligatoire au-delà de `HORIZON_Z`** (`world.js`, `entities.js`) : passé ce point la projection se replie (deux profondeurs pour une même ordonnée) et les objets lointains réapparaîtraient à l'envers sous l'horizon.
  - Vérifié numériquement : la route s'arrête à y≈334 au lieu de l'horizon plat y≈244 (donc ~90 px de ciel gagnés) et conserve **75 px de large** à son extrémité — elle disparaît par-dessus la crête au lieu de se réduire à un point.
- ✅ **Deux réglages faits après contrôle visuel** : `FACADE_PALETTE` nettement éclaircie et `HAZE_STRENGTH` 0,6 → 0,45 — mélangés au rouge saturé de la charte, les gris moyens précédents viraient au **brun boueux** et la ville perdait son aspect béton. Et palier d'aplat rouge ajouté à 94 % du dégradé de ciel, sans lequel la transition orange→rouge mangeait toute la bande et l'horizon restait orange.
- ✅ **120 fps stables** en preview mobile malgré la refonte (aucun coût : la courbure n'ajoute qu'une multiplication par point projeté, et le culling réduit même le nombre de bâtiments dessinés).
- ⬜ À valider sur iPhone : rendu réel des couleurs (les écrans OLED saturent), lisibilité du blanc sur bitume en plein soleil, et confort de la nouvelle distance de vue (l'horizon proche laisse moins de temps pour réagir aux objets).

**Refonte UX/UI des écrans hors-jeu (retours de playtest) :**
- 🎯 **Problème central** : « il y a marqué *Inclinaison activée*, mais je ne comprends pas où je peux démarrer le jeu ». La zone de départ était **l'écran entier** (`window` + `pointerdown`), donc invisible : plus il y avait de boutons à l'écran, moins on devinait qu'il fallait taper *ailleurs*. Le texte « Touche l'écran pour jouer » était dessiné dans le canvas et occupait le centre sans être cliquable.
- ✅ **Écrans hors-jeu repassés en DOM** (`#overlay` dans `index.html`) : un seul conteneur pour le menu **et** l'écran de fin (le titre devient le résultat, le bouton principal devient « REJOUER »). `hud.js` perd `renderStartPrompt`/`renderEndScreen`/`wrapText` (~90 lignes de code mort supprimées) et ne garde que le HUD de jeu. Bénéfices : vrais boutons (cibles ≥48px, recommandation iOS), fondus en CSS plutôt qu'en canvas, et **une action = un bouton**. `#replay-button` disparaît, absorbé par le bouton principal.
- ✅ **Bouton JOUER explicite**, désactivé tant que le morceau n'est pas chargé. Le démarrage au tap n'importe où est supprimé (c'était la cause du problème).
- ✅ **Chargement 0 → 100 %** (`audio.js`) : la réponse est lue **en flux** (`res.body.getReader()`) au lieu d'attendre l'`ArrayBuffer` complet, seule façon d'avoir une progression. 90 % pour le téléchargement, les 10 derniers pour le décodage (qui n'expose rien). Repli sur `arrayBuffer()` si le corps n'est pas lisible en flux ou si `Content-Length` manque. Motif : ~10 Mo sur data mobile, « Chargement… » laissait croire que c'était bloqué.
- ✅ **Fondus partout** : overlay en fondu d'ouverture ET de fermeture, blocs internes décalés de 80 ms chacun (l'écran se compose au lieu de surgir), HUD de jeu monté en fondu sur 0,6 s (`hudAlpha`), écran de fin retardé de 0,6 s pour laisser voir l'instant de la collision. `prefers-reduced-motion` respecté.
- ✅ **Police d'affichage Flawsome** (2e police fournie) sur le titre, Stage Grotesk conservée pour toute l'interface — la hiérarchie titre/interface devient lisible d'un coup d'œil. 216 ko de polices au total.
- ✅ **Volume déplacé** : le curseur vertical collé au bord droit (« pas très intuitif ») devient un **curseur horizontal dans le menu**, réglé une fois avant de jouer. Ne reste en partie qu'une **bascule son ronde** en bas à droite, hors de l'axe de lecture de la route.
- ✅ **CTA « Écouter le morceau » déplacé** en bas de l'overlay (demandé : « on va le mettre autre part ») : présent, mais sans disputer la place au bouton JOUER.
- ✅ **Halo de ramassage plus tenu** : 0,35 s → 0,9 s, avec une vraie enveloppe (montée sur 18 % puis descente en cosinus) au lieu d'une simple décroissance — il « disparaissait trop vite » pour se lire.
- ✅ **Vitesse relevée** : `vitesseBase` 1.0 → 1.15, `vitesseMax` 1.8 → 1.95 (mesuré : 11,0 → 12,9 u/s en jeu).
- Vérifié en preview : cycle complet menu → partie → fin → rejeu, overlay et bascule son correctement montrés/masqués à chaque étape, titre/score/bouton de l'écran de fin corrects, 120 fps, aucune erreur console.
- ⬜ À valider par l'artiste : la police Flawsome est une **didone à empattements**, très éloignée du grotesque de l'interface — c'est un parti pris fort sur le titre, à confirmer.

**⚠️ À savoir pour la prochaine session (pièges déjà rencontrés) :**
- **Ne jamais faire du « tap n'importe où » une action de menu.** C'est ce qui a rendu le démarrage introuvable dès qu'un second bouton est apparu à l'écran. Une action = un bouton visible.
- **Mesurer l'état du jeu depuis une promesse bloquante fausse les résultats** : la preview automatisée bride `requestAnimationFrame` pendant l'attente (`distance = 0.0` alors que la partie tournait ; 147 u confirmés à la capture suivante). Toujours recouper avec une capture d'écran.
- **Le canvas ignore les variables CSS.** Toute couleur/police de charte existe en double : `:root` dans `index.html` (DOM) et constantes de module dans `hud.js` (canvas). Les faire diverger est le piège évident.
- **Pas de convertisseur woff2 sur la machine** (`woff2_compress`, `fonttools`, `pyftsubset` tous absents) : les polices sont servies en `.otf` brut. Installer `fonttools` diviserait leur poids par ~2 — à faire à l'étape 8 (perf).
- **Un bloc conditionné à une capacité absente en preview desktop n'est PAS testé.** Le plantage ci-dessus est passé en preview parce que sa condition (`requestPermission` disponible) est fausse sur desktop. Pour tout code sous `if (capacité mobile)`, simuler la capacité **avant le chargement du module** (script inline temporaire dans `index.html`), pas depuis la console après coup.
- **Tout contrôle superposé au canvas doit stopper `pointerdown`**, pas seulement les `touch*` : c'est `pointerdown` qu'écoute le geste de démarrage global, et il précède `click`. Le patron complet est celui de `#cta-link` (pointerdown + touchstart/move/end).
- **Un refus de permission iOS est silencieux** : `requestPermission()` résout à `"denied"` sans lever d'erreur et, si le réglage Safari global est coupé, **sans afficher de popup**. Ne jamais conclure « la demande n'a pas été faite » sans afficher l'état à l'écran.
- **Tester un module ES sur la preview desktop** : `import('/src/input.js')` depuis la console donne la **même instance** que celle utilisée par le jeu (cache ESM de Vite) — très pratique pour piloter et inspecter un module réel. Attention : un stub global posé ainsi (`window.DeviceOrientationEvent`) **persiste** jusqu'au rechargement de la page.
- **Le gyroscope exige HTTPS.** `mkcert` n'est pas installé sur la machine, mais **`cloudflared` l'est** (`/opt/homebrew/bin/cloudflared`). Commande : `cloudflared tunnel --url http://localhost:5173` → URL `https://<mots-aléatoires>.trycloudflare.com`, déjà autorisée par `allowedHosts` dans `vite.config.js`. **L'URL change à chaque lancement** et le tunnel expose le serveur de dev publiquement le temps du test — à couper après.
- **Vite sert le dossier `public/` depuis un cache de listing** : renommer/supprimer un fichier dedans ne se voit pas immédiatement dans les réponses HTTP (vérifié : un `mv` du symlink mp3 renvoyait toujours 200 avec la bonne taille). Ne pas compter là-dessus pour simuler une erreur de chargement — passer par un stub côté page.
- **Le serveur de dev meurt régulièrement** dans cet environnement (arrivé ~5 fois en une session) — symptôme : le jeu ne charge plus rien sur le téléphone. Relancer avec le skill `run` ou `npm run dev`, ce n'est **pas** un bug du jeu.
- **L'IP locale change si le Wi-Fi change** (vu deux fois en une session : `192.168.1.28` → `192.168.31.86` → revenu à `192.168.1.28`). Toujours revérifier avec `ipconfig getifaddr en0` avant de donner l'URL LAN à l'utilisateur — ne jamais réutiliser une IP d'une session précédente sans la revalider.
- ~~Pas de bouton "rejouer" après un game over~~ → ajouté (voir plus haut).
- **Port 5173 parfois déjà pris** (une session précédente encore ouverte) : Vite bascule alors sur 5174 tout seul. `.claude/launch.json` a `"autoPort": true` depuis cette session pour que l'outil de preview suive ce basculement au lieu de rester bloqué sur 5173.
- **Éditer le code pendant que le jeu tourne sur le téléphone du testeur déclenche un hot-reload (HMR) live** qui peut casser une partie en cours (canvas + audio ne survivent pas toujours proprement au remplacement de module à chaud). Ce n'est pas une régression : un simple rechargement de page suffit. Prévenir le testeur avant d'éditer si une partie est en cours.
- **Le panneau de preview navigateur est visible par l'utilisateur en direct**, y compris les états de debug activés par le modèle pour son propre test (touche `d` : grille rythmique blanche/orange + overlay FPS/position). Ça peut être pris pour un bug par l'artiste en train de regarder ("des lignes bizarres apparaissent"). Penser à repasser `d` pour désactiver le mode debug avant de laisser la preview affichée/sans surveillance, ou prévenir que c'est un outil de dev.
- **La preview navigateur affiche un gros curseur natif orange en haut à gauche de l'écran**, qui bouge même avec les flèches clavier — ressemble à un contrôle média/scrubber natif du navigateur de cet outil (probablement lié au slider volume tourné à -90° + lecture audio active), mais confirmé **hors du canvas** via `ctx.getImageData()` : il n'existe nulle part dans le code du jeu. Ne pas le confondre avec l'indicateur de direction (giro) ajouté cette session, qui lui est bien dans le canvas, correctement positionné/dimensionné (vérifié pixel par pixel) — juste masqué/brouillé par cet artefact sur les captures de cet outil précis. À revalider sur vrai Safari iOS où cet artefact ne devrait pas apparaître.
- **La preview navigateur automatisée (agent) bride sévèrement `requestAnimationFrame`** dès que les appels s'espacent (temps de réflexion entre deux captures d'écran, etc.) : l'horloge audio continue en temps réel mais la boucle de jeu à pas fixe avance au ralenti (observé : ~250s d'horloge audio pour ~35 unités de distance parcourue, alors que la vitesse affichée impliquait un rythme bien plus rapide). Résultat : score/vies semblent ne jamais progresser alors que le morceau défile normalement, et la partie peut atteindre "Parcours terminé" avec un score resté à 0. C'est un artefact de cet environnement de test précis, pas un bug du jeu — sur un vrai appareil (écran actif, onglet au premier plan), la boucle tourne à plein régime et audio/simulation restent synchronisés. Pour vérifier une mécanique de collision précisément dans cette preview, interagir en rafale rapprochée plutôt que d'espacer les captures.

**Revue UX + démarrage étape 7 (session mockup + backend) :**
- 🎨 **Mockup de revue UX livré en artifact HTML** (pas Figma — précision demandée par l'artiste : les outils Figma dont je dispose importent un fichier Figma existant vers du code, ils ne créent rien dans Figma. L'artifact HTML, rendu avec les vraies polices/couleurs du jeu, en est le substitut). Comparatif actuel/proposition sur les 3 écrans hors-jeu (menu, HUD en jeu, écran de fin). Les 3 pistes ont été validées par l'artiste et implémentées dans la foulée (voir ci-dessous) — l'artifact reste comme trace de ce qui a été proposé/pourquoi.
- ✅ **Menu** (`index.html`, `main.js`) : statut de permission d'inclinaison rendu silencieux tant qu'aucune tentative n'a eu lieu (`tiltRequested`, évite le mur de diagnostic technique avant même d'avoir tapé le bouton), CTA "Écouter le morceau" passé en bouton contour (`#cta-link`, classe `.cta-minimal` en réserve pour l'écran Game Over — voir plus bas).
- ✅ **Menu restructuré en 3 étapes séquentielles** (demandé explicitement : « d'abord les gens mettent leur pseudo, ensuite page inclinaison, ensuite page volume/jouer ») — `#onboarding` dans `index.html`, logique dans `main.js` (`stepOrder`/`showStep`/`nextStep`). Étape 1 = pseudo, étape 2 = inclinaison (sautée automatiquement sur les plateformes qui n'ont rien à demander, Android/desktop — `needsTiltPermission`), étape 3 = volume + JOUER. Chaque étape rejoue sa propre petite animation d'entrée à chaque "Suivant". Tagline "Un run au coucher de soleil…" retirée du menu (demandé explicitement), `#menu-sub` supprimé (devenu mort).
- ✅ **Décompte 10 → 1 avant le lancement de la course** (demandé explicitement), avec le message "Réalise le meilleur score, et gagne le vinyl de l'EP" affiché pendant. Piège iOS évité : `audio.js` a dû être scindé en `unlock()` (contexte + buffer muet, appelé synchrone dans le tap JOUER — le geste obligatoire pour iOS) et `play()` (la vraie lecture, appelée à la fin du décompte 10s plus tard — plus besoin d'un geste à ce stade puisque le contexte est déjà débloqué). Sans cette séparation, retarder `unlockAndStart()` de 10s aurait fait perdre le statut de "geste utilisateur" aux yeux d'iOS. Le décompte ne s'applique qu'au premier lancement (bouton JOUER) — REJOUER reste un redémarrage instantané, sans décompte, décision prise pour ne pas ralentir l'enchaînement des parties.
- ✅ **HUD en jeu** (`hud.js`) : score/vies/énergie fusionnés en un seul panneau (largeur mesurée au contenu réel via `measureText`, plus de second îlot à surveiller), vies en cœurs pleins/vides (`heartPath`, silhouette à 4 courbes de Bézier) plutôt qu'en points de couleur, micro-libellé "ÉNERGIE" ajouté au-dessus de la barre. Indicateur giro atténué à 35 % d'opacité par défaut, remonte à 100 % quand l'inclinaison approche du max (`gyroAlpha()`) — reste un filet de sécurité sans occuper le regard en continu.
- ✅ **Écran de fin** (`index.html`, `main.js`) : score détaché en gros chiffre tabulaire (`#score-display`/`#score-num`, écho au style "borne d'arcade" déjà retenu pour l'image de partage), statut concours en pastille séparée (`#contest-pill`, teinte rouge si le concours est ouvert). CTA morceau en bouton contour sur "Parcours terminé" (le joueur vient d'entendre le morceau en entier — la meilleure fenêtre pour l'inviter à l'écouter pour de vrai) mais réduit à un simple lien discret (`.cta-minimal`) sur "Game Over" — l'enjeu y est de relancer vite, pas de retenir l'attention.
- ⬜ **Non vérifié en preview navigateur cette session** : le panneau de preview de cet outil est resté bloqué sur un onglet vide (page jamais chargée malgré un serveur Vite qui compile sans erreur — probablement une passerelle de permission de cette session précise, pas un problème côté jeu). Les changements ci-dessus n'ont donc été validés que par relecture de code, pas à l'écran. **Premier réflexe de la prochaine session : recharger le jeu (LAN ou preview) et vérifier ces 3 écrans avant d'aller plus loin.**
- ✅ **Démarrage étape 7 (Supabase)** : `src/net.js` créé (`postScore`/`getTopScores`, PostgREST, se désactive tout seul tant que `CONFIG.apiScores`/`apiScoresKey` sont vides — jamais d'exception qui casserait l'écran de fin). `supabase-schema.sql` à la racine (table `scores`, RLS lecture/écriture publiques, aucun anti-triche — conforme à la décision verrouillée §6). Champ pseudo Instagram ajouté au menu (`#pseudo-input`, facultatif, persisté en `localStorage`, jamais bloquant pour jouer). `endGame()` envoie le score seulement si le concours est ouvert et qu'un pseudo est renseigné.
- ⬜ **Bloqué sur l'URL projet + clé anon Supabase** de l'artiste (il a un compte) pour remplir `config.js` et tester l'envoi pour de vrai — rien n'est câblé au-delà de ça (pas de page de classement, pas d'indicateur "score envoyé" dans l'UI). L'artiste a préféré continuer à tester en local d'abord et faire l'étape 7 plus tard — pas de blocage, `net.js` ne fait juste rien tant que ces valeurs sont vides.
- ✅ **Tunnel HTTPS relancé** (`cloudflared tunnel --url http://localhost:5174`) pour retester le gyroscope, qui exige un contexte sécurisé (refusé en HTTP LAN simple, confirmé par l'artiste ce coup-ci : « permission refusée, page non sécurisée »). `vite.config.js` autorise déjà `.trycloudflare.com` (`allowedHosts`). Rappel : l'URL change à chaque lancement du tunnel, à ne pas réutiliser d'une session à l'autre.

**4 retours après le 1er test du nouveau parcours (demandés explicitement) :**
- ✅ **Rappel son pendant le décompte** — le test réel a montré un son coupé par l'interrupteur physique sans que rien ne le signale avant le lancement. `#countdown-sound` ajouté (`index.html`) : « Vérifie ton son : interrupteur sonnerie sur iPhone, volume monté sur Android », sous le message d'accroche, discret.
- ✅ **Personnage gardé visible pendant le décompte** — le voile sombre standard de `#overlay` (jusqu'à 92 % de noir) masquait le cycliste pile au moment où il faut au contraire bien le montrer prêt à partir. Nouvelle classe `.countdown-view` sur `#overlay` (posée par `setView()` dans `main.js`) : fond très allégé pendant le décompte uniquement ; texte du décompte repassé avec une vraie ombre de contraste pour rester lisible sur un fond clair (route/ciel) au lieu du halo rouge seul.
- ✅ **Sensibilité inclinaison +10 %** (`config.js`) : `sensibiliteDirection` 1.25 → 1.375.
- ✅ **Étoiles bonus aériennes liées au saut** (`entities.js`) — le bonus étoile (déjà le plus rare/gros des 5 bonus) devient le seul bonus **aérien** : ramassable uniquement en sautant (`isJumping`, même mécanique que la trottinette mais inversée — le saut permet le ramassage au lieu de l'éviter), rendu surélevé à la hauteur du pic de saut du joueur (`HEIGHT_WORLD` importé de `player.js`), avec une ombre au sol comme seul indice visuel qu'"il manque quelque chose" à cet endroit. Donne au bouton saut une vraie raison positive d'exister, au-delà d'éviter un seul type d'obstacle. Type de bonus inchangé (toujours les 5 verrouillés au brief), aucune nouvelle icône nécessaire.
- 💬 **Question ouverte, pas encore tranchée : ligne d'arrivée dédiée (ex. à 80 objets) vs. fin calée sur la fin du morceau.** L'artiste hésite. Recommandation donnée (pas encore implémentée, en attente de confirmation) : garder la fin calée sur la durée du morceau — c'est la prémisse du projet entier (parcours fini = durée du morceau, `dureeMorceau`, spawns calés sur les beats), et une fin anticipée à 80 objets couperait potentiellement le morceau avant la fin, ce qui va à l'encontre de l'objectif « donner envie d'écouter le morceau ». Si le vrai problème est que la fin actuelle ne *ressemble* pas assez à une ligne d'arrivée (plutôt qu'un besoin de la déplacer), un effet visuel de ligne d'arrivée franchie pile à la fin du morceau serait la piste à creuser plutôt qu'un changement de condition de fin.

**Retour en arrière sur le gyroscope — bande de pilotage tactile (demandé explicitement) :**
- 🔄 **Gyroscope entièrement retiré.** Après plusieurs sessions à fiabiliser la permission iOS (tunnel HTTPS, bouton dédié, diagnostic à l'écran — voir sessions précédentes), l'artiste a préféré revenir à un contrôle tactile. Toute la logique gyro supprimée (`input.js` : permission, calibrage, diagnostic ; `hud.js` : indicateur de direction ; `debug.js` : lignes de diagnostic gyro dans l'overlay `?debug` ; `main.js`/`index.html` : étape "Activer l'inclinaison" du menu). `CLAUDE.md` (décision verrouillée) et ce fichier (§0, §2) mis à jour en conséquence.
- ✅ **Nouveau contrôle : bande de pilotage tactile** (`#steer-control`, `index.html`/`input.js`), fixe en bas de l'écran plutôt qu'un glissement "n'importe où" (repli précédent) — toucher un point de la piste y envoie directement le curseur, comme un vrai slider (position absolue, pas un écart depuis le point de départ du geste). Curseur visuel qui suit le doigt en direct.
- ✅ **Bouton SAUT réintroduit visible** (`display:none` retiré) : masqué depuis plusieurs sessions ("je veux que les gens tournent"), mais désormais nécessaire — sans lui un joueur mobile n'a aucun moyen de déclencher un saut, et les étoiles bonus (ajoutées cette session, voir plus haut) ne se ramassent qu'en sautant. Repositionné à côté de la bande de pilotage, même ligne.
- ✅ **Bouton son déplacé en haut à gauche** (zone libérée par l'ancien indicateur giro) — la bande de pilotage occupe maintenant tout le bas de l'écran, plus de risque de collision entre les deux contrôles.

**Hébergement définitif (Netlify) + déploiement en ligne :**
- ✅ **Site Netlify créé** : `pmc-la-ville-est-belle.netlify.app` (HTTPS d'office → gyroscope fiable si un jour on le réintroduit, Web Share OK, plus besoin de tunnel `cloudflared`). Team "La Ville est Belle", project ID `ea72e3f5-4d7a-40e2-bf5e-d96daa72d23c`. Public par défaut (aucun mot de passe, aucun log-in requis pour jouer — seule l'admin console `app.netlify.com/projects/...` demande d'être connecté).
- ✅ **Token Netlify** stocké mentalement dans l'historique de conversation (pas de fichier sur disque). Cycle de déploiement à chaque itération : `npm run build` → `netlify deploy --prod --dir=dist --site=<id>`, ~1 min 30 pour aller de la commande à l'URL live. L'URL reste stable — pas de nouveau lien à donner à l'artiste à chaque fois.
- ⚠️ **Attention cache Safari mobile** : après un redéploiement, un ⌘R/reload forcé est nécessaire pour voir la nouvelle version (Safari mémorise agressivement les JS/CSS bundlés). Le nom de fichier bundle change à chaque build (`index-<hash>.js`), donc en théorie le cache est invalidé automatiquement — mais `index.html` lui reste mis en cache. À prévoir dans la doc de test.

**Voitures en faux-3D (crash au saut + tailles + optimisation) :**
- 🐛 **Crash à répétition** signalé au playtest « ça a crash quand j'ai sauté des voitures » : dans `renderCar3D` (`entities.js`), quand le pare-chocs avant (`zNear = cz - CAR_HALF_L`) passait sous 0, `road.project()` faisait `focal/z` avec z négatif → échelle infinie/NaN, polygones qui débordaient de l'écran, crash silencieux du rendu. ✅ Garde défensive ajoutée : `if (zNear < 0.3) return`. Visuellement identique (la voiture a déjà quitté le champ), mais plus de valeurs infinies.
- ✅ **Voitures 1,5× plus grosses** (demandé explicitement) : toutes les dimensions multipliées par 1,5 (`CAR_HALF_W` 0,75→1,125, `CAR_HALF_L` 0,85→1,275, `CAR_ROOF_H` 0,9→1,35, `CAR_BODY_H` 0,55→0,825, `CAR_SPACING` 2,2→3,3 pour ne pas se chevaucher). `CAR_ROOF_H` reste sous l'apex de saut (≈ 2,28 u avec `hauteurSaut = 2.0`) — la voiture est toujours atterrissable. `ROOF_HALF_WIDTH` dérivée de `CAR_HALF_W` (single source of truth pour la tolérance de posé sur le toit).
- ✅ **Anti-chevauchement des spawns** (playtest : « conflit avec les objets + elles peuvent être présentes au même endroit »). Deux règles ajoutées à `slotContent()` :
  - Buffer de `LARGE_BUFFER_SLOTS = 2` créneaux vides après tout slot « large » (convoi voiture ou bus, `LARGE_KINDS`). Le convoi prend plusieurs unités de profondeur, rien ne doit spawner juste derrière.
  - Deux slots larges consécutifs interdits (deux convois collés OU un convoi + un bus se recouvrant → refus).
  - Implémentation : fonction pure `rawSlotContent()` séparée de `slotContent()` pour ne pas recurser à l'infini quand un slot regarde ses voisins.
- ✅ **Optimisation cache visibleSlots** : `update()`, `roofOverlap()` et `render()` appelaient tous `visibleSlots()` sur la même horloge/vitesse → 3 itérations complètes de la fenêtre (~11 slots × hash chacun) par frame pour rien. Cache par frame (`slotsFor(now, speed)`) invalidé implicitement dès que `(now, speed)` change, et explicitement par `reset()`/`forceSpawn()`.
- ✅ **Rendu voiture en vrai faux-3D** (`renderCar3D`, `entities.js`) : 8 sommets projetés via `road.project()` (4 au sol + 4 sommets d'habitacle rentré latéralement/longitudinalement pour les piliers), 5 faces peintes dans l'ordre arrière → toit + faces latérales visibles selon signe de `cx`. Remplace l'ancienne icône plate `OBSTACLE_ICONS.voiture` (supprimée). Ombre au sol elliptique, feux arrière et reflet clair sur l'arête arrière du toit pour lire "on peut sauter dessus".

**Bus (autocar) — nouvel obstacle :**
- ✅ **Nouveau type** dans `OBSTACLE_TYPES` : `bus`, en plus de `voiture`/`pieton`/`cone`. Dimensions typiques d'un autocar (~12 m × 2,5 m × 3 m) transposées à l'échelle du jeu (`BUS_HALF_W = 1.25`, `BUS_HALF_L = 3.2`, `BUS_ROOF_H = 2.6`). Trop haut pour l'apex de saut — pas atterrissable, à contourner uniquement.
- ✅ **Toujours arrêté sur un côté** (jamais au centre) : `slotX()` reconnaît `content.kind === "bus"` et force `x = ±BUS_SIDE_OFFSET` (≈ 2,55 u du centre, collé au trottoir) selon le hash. Le joueur a toujours une voie complète pour passer de l'autre côté.
- ✅ **Collision spécifique** dans `update()` : fenêtre longue (`BUS_HALF_L`) pour tenir compte de la taille du bus, hitbox latérale un peu plus large que le visuel (`BUS_HIT_HALF_W = BUS_HALF_W + 0.3`, marge défensive). Ajouté à `LARGE_KINDS` → même règle de buffer que le convoi voiture.
- ✅ **Rendu `renderBus3D`** (`entities.js`) : boîte rectangulaire (pas d'habitacle rentré comme la voiture — un autocar a un habitacle qui va d'un bout à l'autre), avec bandeau de vitres claires sur la longueur (5 vitres séparées par des piliers) et lunette arrière : c'est ce qui différencie visuellement le bus d'un simple pavé rouge.

**Vitesse d'accélération ×2 (playtest : « ça va pas assez vite ») :**
- ✅ `config.js` : `vitesseBase` 1,15 → 2,3 et `vitesseMax` 2,6 → 5,2 (doublés tous les deux). La courbe reste la même (`2^(t/90)` dans `road.js`) mais tous ses points sont doublés — sensation "plus rapide partout", pas juste "monte plus vite".

**Séquence de fin (150 objets franchis) — caméra qui freine + perso qui s'éloigne :**
- 🎯 **Demande explicite** : « ligne d'arrivée après 150 objets, arrête la caméra de manière smooth mais laisse le personnage partir loin devant et disparaître à l'horizon ». Avant, franchir la ligne = gel brutal + écran de fin, aucune transition.
- ✅ **Condition de fin** basculée sur `entities.isFinished(now)` (150 objets franchis) au lieu de `now >= dureeMorceau`. Le morceau continue de jouer jusqu'au bout — objectif "donner envie d'écouter le morceau" respecté, seule la course s'arrête. `TOTAL_OBJECTS = 150` exporté par `entities.js`, `finishBeatN()` / `finishTime()` / `objectsPassed()` / `isFinished()` disponibles pour le HUD et l'écran de fin.
- ✅ **État intermédiaire `finishing`** (`main.js`) entre partie jouée et écran de fin. Pendant `FINISH_DURATION = 3.5` s :
  - `road.brake(dt)` (nouvelle fonction dans `road.js`) : décélération douce vers 0 avec `BRAKE_DECAY = 1.2/s`, ~3 s pour arriver à 3 % de la vitesse initiale.
  - Le joueur garde son inertie (`finishing.playerSpeed` décroît à 0,5/s, plus lentement que la route) — sa profondeur monde `finishing.playerZ` monte de `PLAYER_NEAR_Z` (13) vers `HORIZON_Z` (95). Son sprite rétrécit naturellement (`road.project()` gère la mise à l'échelle sans que rien ne change ailleurs).
  - Fondu du sprite sur la dernière seconde (`FINISH_FADE = 1.0 s`) via `ctx.globalAlpha` — le perso disparaît à l'horizon plutôt que de couper net.
  - `entities.update()` court-circuité pendant cette phase : plus aucune collision testée, score/vies figés (le perso est visuellement en train de s'éloigner, ça n'aurait aucun sens de continuer à décrocher des bonus).
- ✅ `restartGame()` remet `finishing` à zéro (`resetFinish()`).

**Écran de fin refait DA — v1 puis v2 (playtest : « ça fait Claude à 100 % vibe coded ») :**
- v1 (première tentative) : score en gros chiffre rouge charte, contest pill en ligne discrète, leaderboard filet fin, 1re place en rouge. Améliorait le score et la typo mais gardait la carte `.panel` modale flottante — le tic UX générique que l'artiste voulait justement casser.
- ✅ **v2 (session en cours)** : suppression complète de la carte `.panel` sur `#end-screen`. Le contenu vit directement sur l'overlay assombri, aligné à gauche (`align-items: stretch`), avec une **barre verticale rouge de charte** à l'aplomb du bloc score (`#end-hero::before`, 4 px de large) comme unique élément structural — remplace le cadre modal.
  - `#menu-title` (« Game Over » / « Parcours terminé ») aligné à gauche uniquement sur l'écran de fin, via `#overlay:has(#end-screen.active)` — Safari iOS 15.4+.
  - Score : chiffre à 84 px 900 rouge charte, letter-spacing serré, label "SCORE" en tracking 24 % au-dessus.
  - Statut concours : ligne à gauche avec point coloré (rouge si concours ouvert, gris uni sinon) — plus de fond de pastille.
  - Classement : titre "MEILLEURS SCORES" aligné à gauche, filets fins entre lignes, 1re place en rouge (rang + points), le reste en blanc/gris tabulaire.
  - Bouton REJOUER : rectangle plein rouge charte à l'aplomb (`border-radius: 0`), pas de bevel, feedback tap par simple `background` plus foncé — sensation "signage" plutôt qu'"application".
- ⬜ À valider par l'artiste : la mise en page éditoriale alignée-à-gauche est un vrai parti pris (rupture visuelle avec le menu, qui garde la carte `.panel` centrée). Si trop cassé par rapport au reste, retour possible à un compromis.
- ✅ **Menu réduit à 2 étapes** (pseudo → volume/JOUER) : l'étape "inclinaison" n'a plus lieu d'être. `stepOrder` n'est plus conditionnel à la plateforme (avant : 3 étapes sur iOS, 2 ailleurs).
- ✅ **Le décompte de 10 s sert maintenant de tutoriel** (demandé explicitement) : la bande de pilotage et le bouton saut sont déjà actifs pendant le décompte (le personnage y répond en direct), un texte dédié l'explique (`#countdown-tutorial` : « Glisse le doigt dans la barre en bas pour diriger — tape SAUT pour sauter »), et le curseur fait un léger va-et-vient ("wiggle", CSS `@keyframes steer-wiggle`) pour attirer l'œil — coupé net dès le premier toucher réel (`input.js` met `animation:none` en inline sur le pouce, pour ne jamais entrer en conflit avec le positionnement en direct pendant un glissement).
- ✅ **Bouton "Passer"** (`#countdown-skip`) ajouté, positionné juste au-dessus de la bande de pilotage (demandé explicitement : "un bouton logiquement placé par rapport au slider") — le décompte/tutoriel est un confort, pas une obligation à chaque partie.
**1er playtest réel du nouveau contrôle tactile — 2 retours traités :**
- 🐛 **Curseur visuel "bloqué au centre" quel que soit l'endroit touché.** Signalé : « j'étais full à gauche et ça reste bloqué au centre ». Cause : `updateThumb()`/`resetThumb()` (`input.js`) et le wiggle du tutoriel (`@keyframes steer-wiggle`, `index.html`) positionnaient le pouce avec `transform: translateX(N%)` — un pourcentage CSS dans `transform` est relatif à la largeur de l'élément **lui-même** (40px), pas à celle de la piste. Le pouce ne parcourait donc que quelques pixels au lieu de toute la largeur du contrôle. Le pilotage réel fonctionnait déjà (score obtenu en jeu), seul le retour visuel mentait. ✅ Corrigé : calcul en pixels réels dérivés de `steerControl.getBoundingClientRect().width`, le pouce atteint maintenant vraiment les bords de la piste.
- 🐛 **Décompte illisible/encombré** : les 3 messages (`#countdown-msg`/`#countdown-tutorial`/`#countdown-sound`) s'affichaient tous en même temps, empilés par-dessus le personnage. Signalé : « mets les phrases les unes après les autres avec fade et surtout plus lisible ». ✅ Corrigé : fusionnés en un seul `#countdown-caption`, qui alterne les 3 messages en fondu sur les 10 secondes (~4s hype, ~3s tuto, ~3s rappel son — `updateCaption()`, `main.js`), avec un fond translucide propre derrière le texte pour garantir la lisibilité quel que soit le décor (l'ombre de texte seule ne suffisait pas sur les zones claires du ciel).
- 💬 **Question posée : « pas de classement à la fin ? »** Ce n'est pas un bug — aucun classement/leaderboard n'a jamais été construit à l'écran : l'étape 7 (backend) n'est câblée que côté envoi (`net.js` poste le score si le concours est ouvert et qu'un pseudo est renseigné), et l'artiste a choisi de continuer à tester en local avant de fournir les clés Supabase (voir plus haut, "on mettra en ligne après"). Sans backend connecté, il n'y a littéralement aucune donnée à classer. Le plan (§6) prévoyait la console Supabase comme tableau de bord suffisant au départ, pas un écran de classement in-game — à confirmer si l'artiste en veut un dans le jeu une fois les clés fournies.
- ⬜ **Toujours pas vérifié par le modèle** (panneau de preview inutilisable cette session) — les 2 correctifs ci-dessus n'ont été validés que par lecture de code, pas à l'écran. À reconfirmer par l'artiste au prochain test.

**Reste à faire :** (1) reconfirmer sur iPhone les 2 correctifs ci-dessus (curseur + décompte) ; (2) clarifier si un vrai écran de classement in-game est voulu pour l'étape 7, ou si la console Supabase suffit ; (3) trancher la question ouverte ligne d'arrivée vs. fin de morceau (voir plus haut) ; (4) récupérer l'URL/clé Supabase quand l'artiste sera prêt ; (5) confirmer sur iPhone réel le correctif audio/horloge d'une session précédente ; (6) étape 8 (polish/perf/livraison) — voir §8.

**Fichiers réellement en place** (voir §3 pour le détail) : `config.js`, `index.html`, `vite.config.js`, `.claude/launch.json`, `supabase-schema.sql`, et dans `src/` : `main.js`, `clock.js`, `road.js`, `world.js`, `entities.js`, `input.js`, `debug.js`, `audio.js`, `player.js`, `hud.js`, `net.js`. `public/assets/la-ville-est-belle.mp3` est un symlink vers `assets/` (nécessaire pour que Vite serve le fichier ; le WAV master n'est volontairement pas exposé).

**Rien de tout ça n'a encore été testé sur un vrai iPhone** — uniquement en preview navigateur (émulation mobile), et même ça a été impossible cette session (voir plus haut, panneau resté bloqué). Le contrôle tactile en particulier n'a jamais été essayé en vrai — c'est tout nouveau cette session.

**Refonte DA des menus/interactions — carte "arcade" à bandeau (demandé explicitement, référence : capture d'écran d'un popup d'un autre jeu mobile) :**
- 🎨 **Demande** : donner aux menus/popups/boutons le langage visuel de la référence envoyée (carte à bordure nette, bandeau d'en-tête coloré, bouton principal à fort contraste, croix de fermeture) — appliqué à **l'ensemble** des écrans hors-jeu et des contrôles en jeu, pas juste un écran isolé. Couleurs de la référence (bleu/or/rouge d'un autre jeu) volontairement **non reprises** : traduites dans la charte déjà verrouillée du projet (rouge `#E13E26` / noir / blanc, StageGrotesk) — voir §"Palette d'après la pochette de l'EP" plus haut.
- ✅ **Nouveau composant `.panel`** (`index.html`) : carte à bordure 3px, fond dégradé sombre, ombre portée marquée, bandeau `.panel-header` rouge en dégradé portant l'eyebrow ("Étape 1/2 — Ton pseudo", etc.). Appliqué aux 2 étapes du menu (`#onboarding`) et à l'écran de fin (`#end-screen`). **Le décompte (`#countdown`) n'a volontairement PAS reçu ce traitement** : une carte à fond plein y recréerait exactement le problème déjà corrigé une session précédente ("le voile sombre standard masquait le cycliste" — voir plus haut) ; il garde son voile allégé existant.
- ✅ **Boutons repassés en style "pressable"** (`.btn-primary`, `#cta-link`, `#countdown-skip`, `#jump-button`, `#mute-button`) : tranche de couleur en bas (`box-shadow` décalée) qui s'aplatit au tap (`translateY` + ombre réduite), au lieu du simple `scale(0.97)` plat précédent — plus proche du retour tactile "bouton d'arcade" de la référence. Bordures/contrastes augmentés sur les boutons secondaires (CTA morceau, Passer, son) pour qu'ils restent lisibles sur un décor chargé.
- ✅ **Contrôles en jeu uniformisés** : `#jump-button` passe d'un contour translucide à un remplissage rouge dégradé (même bevel que les boutons DOM — meilleure lisibilité sur un fond de route/façades changeant) ; `#steer-thumb` récupère un dégradé + liseré pour le même effet "3D" ; `#mute-button` gagne un peu plus de contraste de bordure.
- ✅ **Pas de croix de fermeture ajoutée** : aucun écran du jeu n'est un popup dismissible (le menu est un onboarding séquentiel, pas une boîte de dialogue) — une croix aurait été un ajout d'UX sans action réelle derrière. Le reste du langage visuel de la référence (carte à bandeau, contraste, hiérarchie primaire/secondaire) est repris.
- 🐛 **Bug trouvé et corrigé en cours de route** : `.panel` à `max-width: 280px` rognait le champ pseudo et les boutons (`width: 240px` fixes) contre le bord de la carte une fois le padding interne déduit. Passé à `300px`.
- ✅ **Vérifié en preview mobile (375×812, dark)** : les 2 étapes du menu, l'écran de fin (score + statut concours + REJOUER + CTA), et le décompte (tuto + bande de pilotage + saut + son) — tous rendus correctement, aucune erreur console. *Note technique* : le panneau de preview de cet outil a de nouveau démarré bloqué sur un onglet vide (`chrome-error://chromewebdata/`) avant de fonctionner sur un second onglet — comportement déjà répertorié comme un artefact de cet outil précis (voir pièges), pas un bug du jeu.
- ⬜ **Non testé sur iPhone réel** — prochaine étape demandée par l'artiste.

**Prochain playtest iPhone réel — 10 retours traités dans la foulée (demandés explicitement) :**
- ✅ **Ombres portées retirées** (« trop moche ») : `.panel`, `.btn-primary`, `#cta-link`, `#jump-button`, `#mute-button`, `#steer-thumb` perdent leur `box-shadow` de bevel — retour tactile désormais géré par `filter: brightness()` + `scale`/`translateY`, sans ombre. Glow du score (`#score-num`) et halo coloré du décompte (`#countdown-num`) retirés aussi (gardé : une ombre de contraste neutre sur le décompte, nécessaire à la lisibilité sur fond clair).
- ✅ **Bouton "Passer" du décompte supprimé** (« tout se marche dessus, très moche ») — HTML (`#countdown-skip`), CSS et `skipCountdown()`/son listener retirés de `main.js`. Le décompte va maintenant jusqu'au bout à chaque partie (plus de bypass).
- ✅ **Titre masqué pendant le décompte** (`#overlay.countdown-view #menu-title { display:none }`) — "La ville est belle" restait affiché au-dessus du décompte/de la légende, contribuait à l'encombrement signalé ("il faut que tu revoies ça").
- ✅ **Anneaux retirés autour des bonus/obstacles** (`entities.js`, `makeIcon()` ne dessine plus de `stroke`) — demandé explicitement ("enlève les cercles blancs/rouges, c'est moche"). Les 3 obstacles (portière, piéton, trottinette) sont recolorés directement en rouge de charte (`DANGER #e13e26` / `DANGER_DARK #a12c1c`) au lieu de teintes sombres qui se lisaient comme des "trous dans la route" — le signal danger passe maintenant par la couleur de l'objet lui-même. Vérifié en jeu (mode `?debug`, touche `O`) : portière rendue en aplat rouge, sans anneau.
- ✅ **Saut × 2 + bonus aériens × 2** (`config.js`, `hauteurSaut: 1.0 → 2.0`) — la formule de hauteur de l'étoile aérienne (`entities.js`) et celle du saut du joueur (`main.js`) partagent déjà le même multiplicateur, donc les deux doublent ensemble sans code supplémentaire. Vérifié en jeu : étoile nettement plus haute au-dessus du joueur.
- ✅ **Direction beaucoup plus rapide à fond** (`config.js`, `sensibiliteDirection : 1.375 → 2.75`) — répond à « je galère à traverser toute la carte d'un coup ».
- ✅ **Difficulté progressive** : (1) `road.js` — la vitesse ne monte plus linéairement de `vitesseBase` à `vitesseMax` sur toute la durée du morceau mais suit une racine carrée (`Math.sqrt(songProgress)`), pour que la montée se sente dès la 1ère minute au lieu d'être presque plate jusqu'à la toute fin. (2) `entities.js` — la proportion bonus/obstacle n'est plus fixe (75 % bonus) mais glisse vers 40 % au fil du morceau (`bonusRatioAt()`, fonction pure de `beatN` comme le reste du module) : de plus en plus d'obstacles à mesure qu'on avance, en plus de la vitesse. Cadence de spawn (`cadenceSpawnBeats`) volontairement **non touchée** : la faire varier en cours de morceau aurait demandé de repenser l'indexation des créneaux (risque plus élevé pour le temps disponible) — la marge bonus/obstacle + la vitesse suffisent à faire monter la difficulté.
- ✅ **Personnage redessiné** (`player.js`) — remonté par playtest : « on dirait que j'ai deux roues à droite et à gauche et des pieds qui patinent dans le vide ». Cause : les deux pieds (silhouette plate, couleur quasi identique au pneu) étaient dessinés très écartés de part et d'autre de la roue, à la même hauteur que sa jante visible — ça se lisait comme deux petites roues supplémentaires plutôt que des pieds. Corrigé : pieds resserrés près de l'axe central (au lieu de flanquer les bords de la roue), nouvelle couleur de chaussure nettement plus claire (`PAL.shoe`) + semelle claire (`PAL.shoeSole`) qui contrastent avec le pneu sombre, bassin (short) réduit et remonté pour laisser la roue bien visible en dessous au lieu de la recouvrir presque entièrement. Vérifié en zoomant le sprite isolé (import direct du module en console) à la fois en gros plan et à l'échelle réelle du jeu : se lit maintenant comme une roue unique avec des pieds qui pédalent devant, plus comme trois cercles côte à côte.
- ✅ **Classement en fin de partie câblé** (« j'espère qu'il y a un tableau avec les classements ») — nouveau bloc `#leaderboard` sur l'écran de fin (`index.html`), rempli par `renderLeaderboard()` dans `main.js` via `net.getTopScores()` (déjà écrit à l'étape 7, jamais appelé jusqu'ici). Le score de la partie est maintenant relu juste après l'envoi (`net.postScore` puis `net.getTopScores`), pour avoir une chance de le voir apparaître immédiatement. **Reste bloqué sur les mêmes identifiants Supabase que §7** (`CONFIG.apiScores`/`apiScoresKey`, toujours vides) : tant qu'ils ne sont pas fournis, `getTopScores()` renvoie `[]` et le bloc reste masqué — aucune erreur, mais rien à voir tant que ce n'est pas branché.
- 💬 **Ligne d'arrivée : toujours pas implémentée, question rouverte** (« j'ai l'impression que tu n'as pas mis en place de ligne d'arrivée »). Confirmé : il n'y en a pas — la fin de partie est purement temporelle (`clock.now() >= dureeMorceau`), aucune notion de distance totale/franchissement à l'écran. Non traité cette session (risque de reprendre en urgence une partie du modèle de distance pour un ajout surtout cosmétique) — recommandation inchangée depuis la session précédente : un effet visuel de ligne d'arrivée franchie pile à la fin du morceau plutôt qu'un changement de condition de fin, qui irait à l'encontre de l'objectif "durée = durée du morceau".
- Vérifié en preview mobile (375×812, dark, `?debug`) : cycle complet menu → décompte → jeu → game over → rejouer, HUD, bonus/obstacles, écran de fin — aucune erreur console.
- ⬜ **Rien de tout ça encore vérifié sur iPhone réel** — c'est l'étape suivante.

**Session Supabase branché — étape 7 vivante :**
- ✅ **Projet Supabase créé par l'artiste**, référence `lmlltogosjpxkgofpcdy`. Table `public.scores` créée via le SQL Editor à partir de `supabase-schema.sql` (SELECT + INSERT publics via RLS, aucun UPDATE/DELETE possible avec la clé anon).
- ✅ **`config.js` renseigné** avec `apiScores` (`https://lmlltogosjpxkgofpcdy.supabase.co/rest/v1/scores`) et `apiScoresKey` (clé `anon public`, publiable, protégée par la RLS). La clé `service_role` n'a jamais quitté le dashboard.
- ✅ **Vérifié bout en bout via curl** : GET `[]` en 200, POST d'un score `@test_backend/9999` en 201, GET retour du même score avec tri desc en 200, DELETE anon renvoie 204 mais laisse la ligne en place (RLS attendue — sans policy DELETE, les lignes sont invisibles pour la suppression anon). La ligne `@test_backend/9999` a été laissée en tête du classement en attendant que l'artiste la supprime depuis le Table Editor.
- ✅ **Vérifié bout en bout en jeu** (preview mobile, `?debug`, G pour game over) : le bloc `#leaderboard` de l'écran de fin affiche bien « MEILLEURS SCORES → 1. @test_backend 9999 » lu en direct depuis Supabase. Aucune erreur console.
- ⚠️ **Envoi automatique du score : voulu conditionnel** — `endGame()` ne poste QUE si `contestStatus()` renvoie `open: true`. Or `CONFIG.dateOuverture = 2026-09-11`, donc **aujourd'hui 2026-08-05 : le concours est fermé, les scores ne s'écrivent pas** (message "Concours dès le 11 septembre" affiché). La lecture, elle, tourne toujours. Deux options ouvertes à l'artiste (question en attente de tranche) : ouvrir temporairement la fenêtre pour tester des envois, ou attendre le 11 septembre.

**Session « 5 retours playtest » — bouton saut, étoiles Mario, HUD stable, écran de fin, classement centré, son partout :**
- ✅ **Bouton SAUT agrandi** (`index.html`) : 46 → **78 px** (« beaucoup trop petit »). C'est le seul bouton tapé en pleine course, au pouce, sans le regarder. `#steer-control` recule de `66px` à `108px` à droite pour ne pas passer dessous.
- ✅ **Tous les bonus sont des étoiles** (`entities.js`) — les 5 icônes objet (CD, piano, appareil photo, collier, guitare) sont remplacées par une **étoile façon Mario** : aplat jaune, contour sombre épais peint comme une étoile légèrement plus grande (plus fiable qu'un `stroke` à cette résolution), ombre basse pour le volume, et **les deux yeux** — c'est eux qui font lire « étoile de jeu de plateforme » et pas « une forme d'étoile ». Les 5 types du brief restent (mêmes clés, poids de spawn, valeurs de score, mécanique aérienne de `guitare`) : **seule la silhouette change**, la valeur se lit maintenant à la taille (tiers 0.85 → 1.4).
- ✅ **Tailles revues** : `ICON_SIZE` 20 → **40** (sinon une icône 2× plus grosse à l'écran a des pixels 2× plus gros) ; `ICON_WORLD` 1.02 → **2.04** (objets rouges ×2, demandé) ; nouvelle constante `BONUS_ICON_WORLD = ICON_WORLD × 0.7` = **1.43** pour les étoiles (« fais 30 % plus petit », demandé après coup — les étoiles restent +40 % vs. l'origine mais ne mangent plus la route, les obstacles gardent leur ×2).
- ⚠️ **Hitbox volontairement inchangées** (`PICKUP_RADIUS` 1.3 / `HIT_RADIUS` 0.9) — validées en playtest ; une icône de 2,04 u fait 1,02 u de demi-largeur, donc toujours sous le rayon de ramassage. Même choix qu'au précédent agrandissement.
- ✅ **Piétons repris dans la DA du cycliste** (`pedestrians.js`, réécrit) — « que ce soit la même DA que mon perso, parce que là ça marche pas ». L'ancienne version dessinait en **vectoriel à la volée** (rectangles + arcs à la taille écran, contours lissés, t-shirts bleu/jaune hors charte) : à côté du sprite pixel art du joueur, ça se lisait comme deux jeux différents. Désormais **même technique que `player.js`** : sprites 16×26 pré-rendus une fois au chargement (4 outfits × 4 pas de marche = 16 canvas), blit à l'échelle avec `imageSmoothingEnabled = false`, **palette recopiée de `player.js`** (peau, cheveux, pantalon, semelle claire) + rouge de charte pour les hauts et bande claire type « gilet » (signal danger + écho au maillot rayé du cycliste). API inchangée, `entities.js` n'a pas bougé.
- 🐛 **Piétons qui flottaient au-dessus de la chaussée** (`entities.js`) : le dessinateur attend les **pieds** en `y`, on lui passait `p.y - largeur`. Corrigé en `p.y` — d'autant plus visible depuis que les piétons ont doublé de taille.
- 🐛 **« La barre d'énergie et les cœurs bougent quand le score bouge »** (`hud.js`). Cause trouvée : la largeur du panneau était mesurée sur le **texte réel** du score (`measureText`) et le panneau est ancré **à droite** — chaque chiffre gagné poussait donc tout le contenu vers la gauche. Correctif : gabarit fixe de **6 chiffres** (`SCORE_DIGITS`), score aligné à droite dedans. Vérifié à l'écran : bord gauche du panneau identique à 0, 200, 2200, 4400 et 5350 points.
- ✅ **Écran de fin refait sur le design system du menu** (`index.html`/`main.js`) — « qu'il soit cohérent avec l'UX et l'UI du premier écran ». La v2 était une mise en page éditoriale alignée à gauche **sans carte**, délibérément en rupture avec le menu : c'était exactement le problème (deux langages visuels). L'écran de fin réutilise maintenant **le même composant `.panel` + `.panel-header` rouge + `.panel-body`** que les 2 étapes de l'onboarding, et le bouton `.btn-primary` standard sans surcharge locale. Le résultat (« Game Over » / « Parcours terminé ») passe dans l'**eyebrow du bandeau**, à la place exacte où le menu affiche « Étape 1/2 — Ton pseudo » ; `#menu-title` garde le titre du jeu, centré comme sur le menu (surcharge `:has()` supprimée).
- ✅ **Écran de fin « calme »** (2e passe, demandée juste après) : il y avait **quatre aplats rouges** en concurrence (bandeau, score géant, 1re place, REJOUER) + la ligne surlignée. Le rouge est ramené à trois emplois qui ont chacun une raison d'être — bandeau (composant), ligne du joueur (l'info cherchée), REJOUER (l'action). **Score et 1re place repassent en blanc**, score 84 → 58 px : la hiérarchie se fait à la taille, pas à la couleur.
- ✅ **Score du joueur surligné ET centré dans le classement** (`main.js`, `scrollCurrentScoreIntoView`) — « je suis 8e, je veux que ma ligne arrive au milieu de mon écran ». Surlignage en **aplat rouge plein** (avant : teinte à 15 % qu'on cherchait encore) + scroll de la liste positionné à la main.
  - 🐛 1re version fausse : `li.offsetTop` se mesure depuis le premier **ancêtre positionné**, qui n'est pas la liste — la ligne finissait hors champ (constaté : ligne 11 à 271 px dans une fenêtre de 185). Corrigé avec `getBoundingClientRect()` relatif au conteneur.
  - 🐛 2e problème : une liste ne défile pas au-delà de son contenu, donc une ligne parmi les dernières se collait **en bas** au lieu du milieu. Correctif : `padding-bottom` ajouté dynamiquement, uniquement de ce qui manque et uniquement quand c'est nécessaire (aucun blanc visible sinon). Lignes du classement à **hauteur fixe (37 px)** et fenêtre à **5 lignes** (nombre impair = un vrai milieu).
  - `scrollIntoView({block:"center"})` volontairement évité : sur Safari iOS il remonte aussi l'ancêtre scrollable (la page, en `overflow:hidden` ici).
  - Vérifié à l'écran : « 8. fabrice 5350 » surligné en rouge, pile au milieu de la fenêtre du classement.
- ✅ **Bouton son sur tous les écrans** (`index.html`/`main.js`) : `#mute-button` n'était affiché que pendant la course. Il est maintenant visible en permanence (menu, décompte, jeu, écran de fin) — le morceau tourne en fond dès l'étape 2 du menu et ne s'arrête jamais entre deux parties. **`z-index` 10 → 30**, sinon il passe derrière `#overlay` (20) et devient intappable sur les écrans hors-jeu. Les `classList.add/remove("visible")` correspondants ont été retirés de `main.js`.
- ✅ **Lien CTA changé** (`config.js`) : `lienEP` → **https://li.sten.to/la-ville-est-belle** (remplace le linktr.ee). Vérifié dans le DOM.
- ⚠️ **2 scores de test écrits dans la vraie table Supabase** pendant la vérification (pseudo `fabrice`, **2700** et **5350**) — le concours est ouvert (`dateOuverture` = 2026-08-05), donc toute partie terminée avec un pseudo poste pour de vrai. À supprimer depuis le Table Editor (la clé anon ne peut pas les supprimer, RLS). Rappel déjà noté dans `config.js` : vider la table avant le lancement public.
- ⬜ **Pas encore testé sur iPhone réel** — tout ci-dessus n'est vérifié qu'en preview mobile (375×812, dark).

**Session « retours du frère de l'artiste » — 6 retours traités + mise en ligne Netlify :**
- 🐛 **Les objets se peignaient dans le mauvais ordre de profondeur** (« faut faire gaffe que les objets passent les uns devant les autres »). Vrai bug de rendu, pas un réglage : `slotsFor()` renvoie les créneaux par index croissant, donc par profondeur **croissante**, et `entities.render()` les peignait dans cet ordre — un bus à 60 unités se dessinait donc **par-dessus** une étoile à 15 unités. L'algorithme du peintre veut l'inverse. ✅ Parcours de la liste à rebours (du plus loin au plus près), comme le convoi de voitures le faisait déjà en interne. Vérifié à l'instrumentation : sur une frame, le bas des icônes est désormais strictement croissant à l'écran (312 → 361 → 386 px), c'est-à-dire du plus lointain au plus proche.
- ✅ **Plus d'étoiles sur la fin du parcours** (« y a pas beaucoup d'étoiles au bout d'un moment »). `BONUS_RATIO_END` 0,40 → **0,60** (`entities.js`) : la difficulté monte toujours (1,5× plus d'obstacles à l'arrivée qu'au départ) mais la seconde moitié de la course ne se vide plus — c'est justement là que la vitesse rend le ramassage gratifiant.
- ✅ **Deux nouvelles façons de sauter** (« pas facile de sauter quand tu es droitier »). Diagnostic : le bouton SAUT est en bas à droite, or c'est le **même pouce droit** qui tient la bande de pilotage — sauter obligeait à lâcher la direction. Le bouton reste (découvrabilité), mais s'ajoutent : (1) **balayage du doigt vers le haut depuis la bande de pilotage** (`input.js`, seuil 26 px, se réarme si le doigt redescend → deux sauts enchaînables sans lever le doigt, et la direction continue de suivre l'axe X pendant tout le geste) ; (2) **tap n'importe où dans la zone de jeu** (listener sur le **canvas**, pas sur `window` : bande de pilotage, bouton SAUT et bascule son sont des frères en position fixe, un toucher sur eux n'atteint jamais le canvas — aucun saut parasite). Ne contredit pas la règle « jamais de tap-n'importe-où » du projet : celle-ci vise les actions de **menu**, ici c'est une action de jeu doublée d'un bouton visible. Vérifié en dispatchant de vrais `TouchEvent` : tap canvas → saut ✅, balayage haut → saut ✅ avec direction conservée (−0,64 avant/après), glissement latéral pur → **pas** de saut ✅ et direction bien suivie (0,99).
- 🐛 **Le tutoriel du décompte n'était qu'une image.** En cherchant où brancher le saut, découvert que `#overlay` (z-index 20) recouvre le canvas et les contrôles (z-index 10) : pendant le décompte, la bande de pilotage et le bouton SAUT ne recevaient **aucun** toucher, alors que tout le décompte est présenté comme un tutoriel jouable. ✅ `pointer-events: none` sur `#overlay.countdown-view` — le décompte n'a plus aucun élément interactif depuis le retrait du bouton « Passer », et la bascule son reste tapable (z-index 30). Vérifié via `elementFromPoint` : pendant le décompte, les touchers atteignent bien `#steer-control`, `#jump-button` et `#game-canvas`.
- ✅ **Objets spéciaux en l'air** (« il faut des objets spéciaux en l'air »). Il n'y avait qu'**un seul** type aérien (`guitare`, 5 % des bonus) — donc quasiment jamais rencontré. Les **deux bonus les plus chers** sont maintenant aériens (`guitare` 500 pts + `collierPerles` 250 pts) et pèsent ensemble **28 %** des bonus au lieu de 5 %. Ajout d'un **flottement lent** (±8 % de la hauteur de saut, calé sur l'horloge musicale) : de loin, l'ombre au sol se confondait avec le bitume et rien ne disait « celui-là est en hauteur ». Le flottement n'entre dans **aucun** test de collision — le ramassage ne dépend que de `x` et de `inAir`.
- ✅ **Accélération plus franche** (« accélère un peu plus vite le rythme »). La période de doublement de la vitesse passe de 90 s à **68 s** (`SPEED_DOUBLING_TIME`, `road.js`) et `vitesseMax` 4,0 → **5,0** (`config.js`, sinon la courbe plafonnait avant la ligne d'arrivée et la montée ne se sentait plus sur la dernière minute). **La vitesse de départ ne bouge pas** — un playtest précédent avait justement fait baisser `vitesseBase` (« trop intense au début ») : c'est la pente qui change, pas le début. t=30 s → ×1,36 (avant ×1,26) ; t=60 s → ×1,84 (×1,59) ; t=112 s (ligne d'arrivée) → ×3,12 (×2,37), soit ~51 u/s au lieu de ~39.
- ✅ **Son coupé sur iPhone : vraie correction, pas juste un rappel** (« j'avais pas de son sur mon tél (mais peut-être ma faute) »). Ce n'était pas sa faute : par défaut iOS classe le Web Audio en catégorie *ambient*, donc **coupé par l'interrupteur silencieux physique**, comme un son d'interface. Aucun réglage dans la page ne pouvait le contourner — d'où les sessions précédentes qui concluaient « c'est le petit switch, il faut le basculer ». ✅ Corrigé avec l'**API AudioSession** (Safari 16.4+) : `navigator.audioSession.type = "playback"` posé dans `unlock()`, juste avant la création de l'`AudioContext` — iOS traite alors le jeu comme un lecteur de musique et **le son sort même en mode silencieux**. Détection de fonctionnalité + try/catch : sans effet et sans risque sur les autres navigateurs (absent en Chrome desktop, vérifié). Le rappel « vérifie ton son » du décompte est conservé (volume Android, écouteurs).
- ✅ **Vérifié en preview mobile (375×812, dark, `?debug`)** : cycle complet menu → décompte → course → game over → rejouer, **120 fps**, `audio=lecture` / `horloge=audio`, aucune erreur console, classement Supabase lu et affiché.
- ✅ **Déployé en production** : `https://pmc-la-ville-est-belle.netlify.app` (build 37,6 ko de JS gzippé 14,8 ko, 11 Mo au total avec le MP3 et les polices). Page de prod rechargée et vérifiée après déploiement. ⚠️ Rappel cache Safari : un rechargement forcé peut être nécessaire sur un iPhone qui avait déjà ouvert le jeu.
- ⚠️ **Un score de test a été écrit dans la vraie table Supabase** pendant la vérification (pseudo `polll`, **350**) — le concours est ouvert (`dateOuverture` = 2026-08-05), donc toute partie terminée avec un pseudo poste pour de vrai. À supprimer depuis le Table Editor, avec les autres lignes de test déjà signalées (`@test_backend`, `fabrice`, `pmc`).
- ⬜ **À confirmer sur iPhone réel** : (1) le son en mode silencieux (c'est LE point à valider, la preview desktop ne peut pas le reproduire) ; (2) le confort des deux nouvelles façons de sauter, en particulier pour un droitier ; (3) la nouvelle courbe de vitesse (« un peu plus vite » est subjectif — dire si c'est trop ou pas assez).

**Session « 4 voies » — refonte du cœur du gameplay + pseudo/insta + son :**
- 🎯 **Passage à 4 voies** (demandé : « le personnage est soit sur la voie 1, 2, 3 ou 4, jamais entre deux voies ni en dehors de la route »). Motif du playtest : « t'es short sur les hitbox » et « si je me mets là, je suis sûr de ne jamais me prendre de bagnole, je peux rester là indéfiniment ». La chaussée (8 unités) est découpée en 4 voies de 2 unités, centres à -3/-1/+1/+3 (`LANE_COUNT`/`LANE_WIDTH`/`laneX()` dans `road.js`, source unique : les séparateurs dessinés à l'écran et la position des objets sortent des mêmes valeurs). 3 lignes blanches pointillées remplacent l'unique ligne centrale.
- ✅ **Le joueur ne connaît plus qu'un numéro de voie.** `playerState.lane` (0..3) est la vérité ; `playerState.x` n'est que la position affichée, qui rattrape le centre de la voie en ~0,18 s (`LANE_TWEEN`, modulé par `sensibiliteDirection`). Sortir de la route est devenu **impossible par construction** et non plus par bornage. L'inclinaison du sprite est dérivée de la vitesse *observée*, donc conservée telle quelle.
- ✅ **Tous les objets sont sur une voie** (`slotLanes()`, `entities.js`), y compris les piétons et les convois. **Le bus occupe DEUX voies** (demandé : « tu peux mettre une voiture en travers qui utilise deux voies ») : `BUS_HALF_W` 1,25 → 1,9, deux voies adjacentes tirées parmi les 3 paires possibles — il peut donc barrer le milieu, pas seulement un bord, et il reste toujours deux voies libres. `CAR_HALF_W` 1,125 → 0,85 pour qu'une voiture remplisse sa voie sans mordre sur les voisines.
- ✅ **Collisions = égalité de voie.** `HIT_RADIUS`/`PICKUP_RADIUS`/`ROOF_HALF_WIDTH` supprimés : on touche ce qu'on voit, et l'atterrissage sur un toit de voiture ne dépend plus d'une tolérance latérale (« je saute pile dessus et je retombe à côté » réglé au passage).
- ✅ **Contrôles entièrement au geste** (`input.js` réécrit) : la bande de pilotage et le bouton SAUT sont **retirés de l'écran** — le jeu se tient à un pouce, « comme une Game Boy verticale ». Swipe gauche/droite = **un cran** de voie (deux swipes pour traverser de la 2 à la 4, comme demandé), swipe haut = saut. Le geste est reconnu au franchissement du seuil (34 px), sans attendre le lever du doigt, et se réarme en cours de course : un swipe long enchaîne deux crans. Un geste qui **démarre sur un écran hors-jeu** est ignoré (sinon faire défiler le classement changerait de voie) — le décompte, lui, reste jouable puisqu'il est en `pointer-events: none`.
- ✅ **Caméra qui suit latéralement** (`road.setCameraX`, `CAMERA_FOLLOW = 0.55`). Trouvé à l'écran pendant le test : sur les voies extérieures, **le sprite sortait à moitié du cadre** (la chaussée de 8 unités déborde de l'écran à la profondeur du joueur). Plutôt que de rétrécir la route — ce qui aurait obligé à re-régler toutes les tailles d'objets validées au playtest — la caméra se décale de 55 % du déport du joueur, comme dans un Subway Surfers. Décalage exprimé en unités-monde, donc parallaxe correcte : le lointain ne bouge presque pas, le premier plan suit. Vérifié aux deux extrêmes (x = ±3, personnage entièrement dans le cadre).
- ✅ **Pseudo et Insta séparés** (« il faut demander pseudo ET insta, et on affiche à la fin le pseudo seulement — sinon tout le monde va voir l'insta du gagnant »). Étape 1/2 : deux champs, **pseudo obligatoire** (bouton Suivant désactivé sans lui, sinon on jouerait pour un score jamais classé), insta optionnel. Les deux persistés en `localStorage` (clés `pseudoJoueur` / `pseudoInsta`).
- 🔒 **L'insta est protégé CÔTÉ SERVEUR, pas seulement à l'affichage.** Point trouvé en implémentant : la clé anon est forcément publique (elle est dans le bundle) et autorisait la lecture de toute la table — masquer la colonne dans l'UI n'aurait rien protégé du tout. `supabase-migration-pseudo-insta.sql` (nouveau, à la racine) ajoute une colonne `pseudo`, recopie l'existant, **retire la policy de lecture publique** et crée la vue `scores_public` (pseudo + score uniquement), seule chose que le jeu lit désormais. `net.js` lit la vue, `postScore(pseudo, insta, score)` écrit les deux.
- ⚠️ **`net.js` a deux replis tant que la migration n'est pas appliquée** : lecture → retombe sur la table si `scores_public` n'existe pas (404) ; écriture → réessaie à l'ancien format si la colonne `pseudo` manque (400). Les deux ont été observés en action pendant les tests (le classement s'affichait, les scores partaient). **Conséquence : des 400/404 apparaissent en console tant que le SQL n'est pas passé — c'est attendu, pas une régression.**
- ✅ **Volume : curseur retiré du menu, menu dépliant en jeu** (demandé : « on supprime dans le menu avant de commencer la partie » / « à la place du bouton volume, il faut un petit menu dépliant »). `#sound-control` en haut à gauche : un tap ouvre un panneau avec le curseur, un tap ailleurs le referme. L'étape 2/2 gagne un **encadré « 🔊 Monte le son »** (« sinon ils ne sont pas au courant que la musique existe »).
- ✅ **CTA « Écouter le morceau » : icône « nouvel onglet »** ajoutée à droite dans le bouton (« ça va enlever la page ? donc je vais jamais cliquer là-dessus »). Le lien était **déjà** en `target="_blank"` — le jeu n'a jamais été perdu, mais rien ne le disait avant le clic. (Lenteur du lien li.sten.to au premier chargement : CDN froid côté leur service, rien à corriger ici — diagnostic de l'artiste confirmé.)
- ✅ **Vérifié en preview mobile (375×812, dark, `?debug`)** : les 4 voies et leurs séparateurs, les 2 étapes du menu, le décompte-tutoriel réécrit, la course, l'écran de fin et le classement. Gestes testés en dispatchant de vrais `TouchEvent` — swipe droite → +1, gauche → −1, haut → saut, bas → rien, micro-mouvement → rien, swipe long → 2 crans ; clamp aux deux extrémités confirmé (x = ±3). **120 fps**, `audio=lecture` / `horloge=audio`.
- ⚠️ **Scores de test écrits dans la vraie table** pendant la vérification (pseudo `testeur`, 350/850) — à supprimer depuis le Table Editor avec les lignes déjà signalées.
- ✅ **Migration Supabase appliquée par l'artiste** (`supabase-migration-pseudo-insta.sql`). Vérifié bout en bout après coup : écriture → 201, lecture publique (`scores_public`) → pseudo + score uniquement (même en demandant `select=*`, l'insta n'existe pas dans la vue), table brute non lisible publiquement → `[]`. Partie réelle jouée sur le site en ligne, score envoyé et affiché correctement. Scores de test nettoyés par l'artiste.

**1er playtest réel sur iPhone (iPhone 16) des 4 voies — 6 retours :**
- ✅ **Confirmé fonctionnel** : les 4 voies, le swipe, le saut, le classement pseudo-seul — le cœur de la refonte tient sur vrai device.
- 🐛 **Musique continue quand on quitte l'onglet/l'app.** Demandé : couper le son (avec fondu 0,5 s) quand l'onglet perd le focus, refondu en revenant — `document.visibilitychange`, pas encore câblé.
- 🐛 **Un swipe fort peut traverser plusieurs voies d'un coup** (« si je swipe très fort, je peux traverser toutes les voies »). Contredit la demande initiale « deux swipes pour la voie 2 à la voie 4 » : nouvelle règle, plus stricte — **un swipe (un contact doigt) = exactement une voie**, quelle que soit la distance/vitesse. Cause identifiée : `input.js` réarme l'origine du geste dès qu'un cran est franchi, permettant d'enchaîner plusieurs crans dans le même contact. À corriger : un seul cran consommé par cycle touchstart→touchend.
- 🐛 **Le panneau volume déplié chevauche le HUD énergie/vies/score** (capture iPhone 16 à l'appui). Cause identifiée : le panneau HUD (`hud.js`) est ancré à **droite** et sa largeur dépend du score affiché ; le panneau volume s'ouvre vers la **droite** depuis le bouton son en haut à gauche — sur la largeur d'un iPhone, les deux se recouvrent. Fix prévu : dérouler le panneau volume **vers le bas** au lieu de vers la droite.
- 🎨 **Priorité artistique n°1 de la prochaine session — bâtiments en vrai volume 3D.** Demandé avec référence photo (immeuble haussmannien parisien, pierre de taille, balcons filants, toit mansardé). Aujourd'hui `world.js` peint des façades **plates, toujours face caméra** (rectangles 2D), y compris sur les bâtiments latéraux — ça casse la perspective et ne rend pas justice au titre du jeu. Demande explicite : les bâtiments doivent se voir **de profil, de manière logique vis-à-vis de la route**, pas comme un décor 2D plaqué. Plan : reprendre le même principe que `renderCar3D`/`renderBus3D` (volumes projetés via `road.project()`, face avant + face de profil qui s'étire vers la fuite) et redessiner les façades en pixel art à partir de la référence. **Reformulé explicitement par l'artiste comme LE point central de la session** (« lis entre les lignes » → le jeu s'appelle *la ville est belle*, la ville doit être visuellement à la hauteur de son nom — pas un point technique parmi d'autres).
- 🎨 **Cyclistes NPC + bus/vélo plus lisibles** (2 images de référence fournies, pixel art à adapter à l'échelle du jeu) : (1) des cyclistes en sens inverse sur certaines voies ; (2) le bus se lit encore comme « un gros bloc rouge », besoin de détails distinctifs (vitres, portes, etc.) ; (3) le vélo du joueur manque de contact/lisibilité avec la route (ombre, jantes), inspiration Pokémon (contraste net plutôt que plat).

**Reste à faire, dans l'ordre convenu avec l'artiste (prochaine session) :**
1. **Bâtiments haussmanniens en vrai volume 3D** — priorité n°1, cœur visuel du projet.
2. Musique en pause hors onglet (fade 0,5 s entrée/sortie).
3. Un swipe = une voie, verrouillé (un seul cran par contact).
4. Panneau volume qui se déroule vers le bas (fin du conflit avec le HUD).
5. Cyclistes NPC + bus lisible + vélo du joueur plus lisible (style Pokémon).

**Session « 5 chantiers du 1er playtest iPhone 16 » — les 5 points traités dans l'ordre demandé :**
- ✅ **1. Bâtiments haussmanniens en vrai volume 3D** (`world.js`, réécrit). Chaque bâtiment était une carte plate à un seul `z` (donc toujours face caméra, comme un décor en carton). Refonte en vrai bloc à deux faces projetées via `road.project()`, même principe que `renderCar3D`/`renderBus3D` (`entities.js`) : (a) la **façade** (le long mur qui longe la rue, `x` fixe à l'intérieur, `z` de `zNear` à `zFar`, nouvelle dimension `depth` par bâtiment) — porte la rangée de fenêtres et un **balcon filant** (bande sombre + piquets, un rang sur deux, demandé par la référence photo) ; (b) le **retour d'angle** (mur pignon perpendiculaire, à `z = zNear`, `x` de `xInner` à `xOuter`, nouvelle dimension `girth`) — c'est lui qui donne la sensation de profil/volume, toujours face caméra puisque c'est le bord le plus proche du bloc. La façade s'étirant en perspective (near/far n'ont pas la même échelle), son détail (fenêtres/corniche/toit) est peint puis **découpé au `ctx.clip()`** sur le vrai quadrilatère projeté plutôt que sur un rectangle axé écran ; le retour d'angle, à `z` constant, est un vrai rectangle (`y` identique aux deux bouts par construction de `project()`) donc pas besoin de clip mais même chemin de code. Toit mansardé/corniche/fenêtres/cheminées désormais en **hauteur-monde** (fraction de `shape.height`) plutôt qu'en fraction de la boîte écran — un changement qui les fait correctement s'aligner entre les deux faces d'un même bâtiment. Un espace (`SPACING - depth`) laissé entre bâtiments consécutifs lit comme des ruelles latérales, renforçant la séparation en volumes distincts.
  - Vérifié en preview mobile (375×812, dark, `?debug`) : volumes clairement lisibles de profil, roulé à travers plusieurs dizaines d'unités de distance sans couture/repliement visible près de l'horizon courbe, façades qui varient bien d'un bâtiment à l'autre, **aucune régression de perf** (`frameMs` ~7–9 ms, dans la même fourchette qu'avant la refonte — le clip supplémentaire par bâtiment ne coûte rien de mesurable).
- ✅ **2. Musique en pause hors focus** (`audio.js`, `main.js`). Nouveau **troisième gain** dans la chaîne audio (`focusGain`, après `envelopeGain`/`volumeGain`, chacun indépendant pour ne jamais se marcher dessus — voir en-tête d'`audio.js`) : `document.visibilitychange` déclenche `pauseForFocusLoss()` (fondu `focusGain` → 0 sur 0,5 s puis `audioCtx.suspend()` une fois le fondu terminé, pas avant — un `suspend()` immédiat aurait coupé la rampe en plein milieu) et `resumeFromFocusLoss()` au retour (`resume()` + fondu → 1 sur 0,5 s). `audioCtx.suspend()` fige aussi `audioCtx.currentTime`, donc `clock.now()` — la partie se **fige vraiment** pendant l'absence, pas seulement le son (cohérent avec `document.hidden` qui arrête de toute façon `requestAnimationFrame`). Piège évité : le chien de garde `AUDIO_STALL_TIMEOUT` (bascule sur l'horloge de secours si l'audio ne bouge pas depuis 1 s) aurait vu une horloge figée depuis tout le temps passé en arrière-plan et basculé à tort **juste au moment de revenir** — `main.js` réinitialise `audioWatch.lastT`/`lastReal` dans le handler `visible` pour éviter ça.
  - Vérifié en simulant `document.hidden` (override de la propriété + `dispatchEvent(visibilitychange)`, même technique que les sessions gyroscope précédentes) : passage à `audio=contexte suspended` après le fondu, `horloge=audio` **conservé** (pas de bascule secours) au retour, `audio=lecture` de nouveau après `resumeFromFocusLoss()`. Non testable autrement que simulé dans cet environnement (pas de vrai "changement d'onglet" possible ici) — **à confirmer sur iPhone réel** (mise en arrière-plan de l'app).
- ✅ **3. Un swipe = une voie, verrouillé** (`input.js`). Cause confirmée : `trackGesture()` réarmait `originX`/`originY` à la position courante dès qu'un cran était reconnu, permettant d'enchaîner plusieurs crans dans le même contact si le doigt continuait sa course. Corrigé avec un flag `gestureConsumed` (posé à `true` dès qu'un cran — voie ou saut — est reconnu, remis à `false` seulement à `beginGesture()`, donc au prochain `touchstart`/`mousedown`) : `trackGesture()` sort immédiatement si le contact a déjà consommé son cran, quels que soient les `touchmove` suivants.
  - Vérifié en dispatchant de vrais `TouchEvent` : un swipe fort et rapide (300 px, plusieurs `touchmove`) en un seul contact → **exactement 1** voie franchie (`x` de −1,00 à +1,00, pas +3,00) ; un second contact séparé juste après → encore exactement 1 voie (+1,00 → +3,00). Le verrouillage se réarme donc correctement à chaque nouveau contact.
- ✅ **4. Panneau volume vers le bas** (`index.html`). `#sound-control` passé de `flex-direction: row` (implicite) à `column` + `align-items: flex-start` (au lieu de `center`) : le panneau se déploie maintenant sous le bouton plutôt qu'à sa droite, et reste ancré à gauche (pas de risque de déborder l'écran côté droit). Animation d'ouverture changée de `translateX` à `translateY` en conséquence.
  - Vérifié en preview mobile : panneau déplié visible entièrement sous le bouton son (haut gauche), aucun chevauchement avec le panneau HUD (score/vies/énergie, haut droit) quel que soit son contenu.
- ✅ **5. Cyclistes NPC + bus/vélo plus lisibles.**
  - **Cyclistes NPC** (`cyclists.js`, nouveau fichier — même technique que `player.js`/`pedestrians.js` : pixel art pré-rendu, géométrie du vélo/cheveux/jambes **reprise à l'identique de `player.js`** pour zéro risque visuel, seul le maillot change de teinte, 4 couleurs recyclées de `pedestrians.js` sans la bande "gilet" — ce signal reste réservé aux obstacles). **Purement décoratifs** : générateur entièrement indépendant du système bonus/obstacle (`entities.js`, `visibleNpcs`/`npcContentAt`, hash à multiplicateurs distincts) pour ne jamais perturber les probabilités déjà réglées — aucun des 3 obstacles verrouillés au brief n'est touché. Apparaissent sur les **voies extérieures** (0 et 3) avec une vitesse de rapprochement **ajoutée** à celle de la route (`NPC_CLOSING_EXTRA`, +7 u/s) plutôt que la même formule que les objets immobiles du monde — c'est ce qui donne la sensation "vient vers moi" plutôt que "posé sur le trajet", cohérent avec "en sens inverse". Rendus **avant** le passage bonus/obstacle dans `render()` : un élément décoratif ne doit jamais recouvrir ce qui compte pour le score.
    - Vérifié : repéré en jeu (silhouette crème near une étoile), confirmé par rendu isolé des 4 teintes de maillot (mêmes proportions que le joueur, ombre de contact au sol, jante à liseré clair), et par relecture de la formule de spawn dans la console (≈35 % des créneaux, cadence 4× plus espacée que bonus/obstacles) — cohérent avec une apparition occasionnelle plutôt qu'envahissante.
  - **Bus plus lisible** (`entities.js`, `renderBus3D`). Le playtest signalait encore "un bloc rouge" malgré les vitres déjà présentes (bande trop fine, 20 % de la hauteur). Élargie à 40 % (0,40–0,80 au lieu de 0,55–0,75), séparateurs de vitres épaissis (1→2 px), **ceinture de caisse** ajoutée (liseré clair sous les vitres, seul détail horizontal qui casse l'aplat rouge), et surtout **deux roues au sol** (ellipses sombres + moyeu clair, avant/arrière) — l'absence de roues était ce qui manquait le plus pour lire "véhicule" plutôt que "pavé de couleur".
  - **Vélo du joueur plus lisible** (`player.js`). Deux retours combinés dans les 2 images de référence (jantes nettes, contact au sol) : **ombre de contact** ajoutée sous la roue (ellipse sombre semi-transparente, non tournée par le lean — une ombre reste plaquée au sol même en virage, contrairement au sprite), et **reflet de jante** en aplat net (pas de dégradé, esprit "contraste franc" des références) plutôt que le simple disque gris uni précédent. Même traitement d'ombre ajouté aux cyclistes NPC pour la cohérence.
    - Vérifié en preview : ombre visible sous la roue du joueur à l'arrêt comme en mouvement, jante nettement plus définie, bus identifiable de loin (roues visibles dès l'approche), aucune erreur console sur l'ensemble de la session.
- ⬜ **Rien de tout ça n'a encore été testé sur iPhone réel** — comme chaque session, c'est la prochaine étape. Point d'attention particulier pour le retour n°2 (pause hors focus) : c'est le seul des 5 qui ne peut PAS être vérifié en preview desktop/mobile émulée (la mise en arrière-plan réelle d'un onglet/app n'est simulable qu'approximativement via `document.hidden` forcé) — à confirmer en priorité sur device.

**Reste à faire (prochaine session) :** confirmer les 5 chantiers ci-dessus sur iPhone réel, en particulier la pause audio hors focus (seul point non simulable fidèlement en preview) et le confort des cyclistes NPC (fréquence/gêne visuelle en conditions réelles de jeu, pas seulement en debug).

**Session « menu pause + glide + mise en ligne » :**
- ✅ **Menu pause avec réglage du son** (demandé explicitement). Nouveau bouton dédié `#pause-button` (rond, bas droite — le bas de l'écran est libre depuis le passage aux contrôles 100 % swipe), visible uniquement en course active (`showPauseButton()`/`hidePauseButton()`, appelées depuis `beginRun()`/`restartGame()`/`endGame()`/`beginFinish()` dans `main.js`). Panneau `#pause-screen` sur le même composant `.panel`/`.panel-header` que le reste du jeu (bandeau "PAUSE", curseur de volume — **réutilise `audio.setVolume`/`getVolume`**, synchronisé en direct avec le curseur du menu dépliant existant —, bouton REPRENDRE, lien discret "Recommencer la course").
  - **La pause fige vraiment la simulation, pas seulement l'affichage** : `frame()` (boucle à pas fixe) n'avance plus l'accumulateur tant qu'`isPaused()` est vrai — `step()` n'est simplement plus appelé, `render()` continue de peindre la dernière frame calculée derrière le voile. Sans ça, le sol (qui défile sur `dt` réel, pas sur l'horloge audio — voir `road.js`) aurait continué à glisser derrière un panneau "pause" qui n'aurait rien mis en pause.
  - **Réutilisation du mécanisme de pause hors focus** de la session précédente : `audio.js` a ses deux fonctions renommées de `pauseForFocusLoss`/`resumeFromFocusLoss` vers `pause()`/`resume()` (génériques, deux appelants désormais — la perte de focus ET le bouton pause), même fondu 0,5 s + `audioCtx.suspend()` qui fige `clock.now()`.
  - **Coordination à deux sources** (`main.js`) : `manualPaused` (bouton) et `hiddenPaused` (`document.hidden`) sont indépendants, combinés via `isPaused() = manualPaused || hiddenPaused` et `applyPauseState(wasPaused)` qui ne déclenche l'audio que sur une vraie transition — sans ça, ouvrir le menu pause PUIS changer d'onglet aurait fait repartir le son au retour sur l'onglet alors que le menu pause est encore affiché.
  - Même garde `stopPropagation` que tous les autres contrôles superposés au canvas (`#pause-screen`, `#pause-button` : pointerdown/touch*/mousedown) — sinon un tap sur le panneau se lirait comme un swipe par `input.js`.
  - Vérifié en preview mobile (375×812, dark, `?debug`) : ouverture → `audio=contexte suspended`, `distance`/`temps` figés pile à leur valeur au moment du tap, curseurs synchronisés (testé à 40 %), fermeture → `audio=lecture` repris, `horloge=audio` **conservé** (pas de bascule secours malgré le gel), la course reprend exactement où elle s'était arrêtée. Aucune erreur console.
- ✅ **Glide de 0,2 s au changement de voie** (demandé explicitement : « un petit délai pour que ça glisse plus »). Cause : `LANE_TWEEN = 16` avait été calibré pour ~0,18 s à `sensibiliteDirection = 1`, mais ce réglage a grimpé à 2,2 au fil de plusieurs playtests **pour une autre raison** (l'ancien pilotage continu, avant le passage aux 4 voies) — en le multipliant tel quel, le changement de voie ne prenait plus que ~0,08 s, un snap quasi instantané plutôt qu'un glissé. Rebasé à `LANE_TWEEN = 7` (`main.js`) pour retomber sur ~0,2 s à la sensibilité actuelle (calcul détaillé en commentaire sur place). Vérifié : un swipe déplace le joueur d'exactement une voie, avec un glissé désormais visible à l'écran (avant : un saut net).
- ✅ **Mis en ligne sur Netlify** (demandé explicitement) : `npm run build` (44,7 ko JS gzippé 16,8 ko) → `netlify deploy --prod --dir=dist --site=ea72e3f5-4d7a-40e2-bf5e-d96daa72d23c`. Live à **https://pmc-la-ville-est-belle.netlify.app**, rechargé et vérifié après déploiement (aucune erreur console, façades 3D bien présentes). ⚠️ Rappel cache Safari : un rechargement forcé peut être nécessaire sur un iPhone qui avait déjà ouvert le jeu avant ce déploiement.
- ⬜ **Rien de tout ça n'a encore été confirmé sur iPhone réel** — en particulier le menu pause (le fondu audio + le gel de la simulation n'ont été vérifiés qu'en preview desktop/mobile émulée) et la sensation du nouveau glide de voie (0,2 s est un choix arbitraire à valider au toucher).

**Reste à faire (prochaine session) :** confirmer sur iPhone réel le menu pause (fondu audio, gel/reprise de la simulation) et le nouveau glide de voie (0,2 s — trop lent/trop rapide ?) ; reste aussi en attente depuis la session précédente : bâtiments 3D, pause hors focus, swipe verrouillé, panneau volume, cyclistes NPC/bus/vélo.

**Session « retours groupés + crédits Netlify + compression audio » :**
- 💬 **Question sur les crédits Netlify** (capture d'écran d'un mail « 50 % des crédits utilisés »). Vérifié directement via l'API Netlify (`netlify api listAccountsForUser`) : plan **Free**, **aucune carte bancaire enregistrée** (`payment_method_id: null`) — donc aucun risque de prélèvement automatique, même à 100 % des 300 crédits du cycle. Cause probable : le MP3 (~10 Mo à l'époque) téléchargé à chaque partie, cumulé sur les tests de la session précédente + les miens. Pas d'action bloquante prise ici, juste vérifié et expliqué — la compression ci-dessous répond au fond du problème.
- ✅ **Pause à la place du bouton Volume** (demandé explicitement). `#pause-button` occupe maintenant exactement l'emplacement de `#sound-control` (haut gauche) au lieu du bas droit ; les deux ne sont jamais affichés en même temps (`showPauseButton()`/`hidePauseButton()` dans `main.js` masquent/affichent l'autre en miroir, avec la règle CSS `[hidden]` à spécificité explicite — même piège que `#pause-button` déjà rencontré). La bascule son reste sur les écrans hors-jeu (menu/décompte/fin) ; Pause la remplace pendant la course, son panneau contenant déjà le volume.
- ✅ **HUD vertical** (`hud.js`, `renderHud` réécrit). Score tout en haut à droite (gabarit de chiffres fixe inchangé, pour ne pas redéclencher le bug historique "les cœurs bougent quand le score change"), cœurs empilés un par un en dessous, jauge d'ÉNERGIE désormais **verticale** (se lit de bas en haut) sous les cœurs — un seul panneau étroit plutôt que le bandeau horizontal précédent.
- ✅ **Voitures en rangées multi-voies, bus supprimé** (`entities.js`, refonte significative). L'ancien "convoi" (3 voitures alignées dans la MÊME voie, l'une derrière l'autre) est remplacé par des **rangées de 1 à 3 voitures sur des voies DIFFÉRENTES à la même profondeur** (`pickLanes()`, permutation déterministe des voies pilotée par hash — jamais deux fois la même voie, jamais les 4 à la fois). Poids de spawn : `voiture` pèse maintenant **60 %** des obstacles contre 20 % chacun pour `pieton`/`cone` (`OBSTACLE_WEIGHTS`) — 3× plus fréquent que les deux autres, combiné à des rangées de plusieurs voitures pour une hausse nette du nombre total de voitures rencontrées. Le **bus est entièrement retiré** (`renderBus3D`, `BUS_*`, la branche de collision dédiée : tout supprimé) — demandé explicitement, son rôle "force à changer de voie" est repris par les rangées.
- ✅ **200 étoiles exactement, chaque partie** (`entities.js`). Nouveau système de **quota exact par diffusion d'erreur** (même principe qu'un tramage d'image) qui remplace l'ancien tirage probabiliste (`hash(slot) < ratio`) : celui-ci était déjà déterministe d'une partie à l'autre, mais le TOTAL d'étoiles qui en résultait n'était pas un nombre choisi à l'avance. `TOTAL_STARS = 200` est désormais LE réglage central ; `TOTAL_OBSTACLES = 100` équilibre en face (compte tenu des voitures tripées) ; `TOTAL_OBJECTS = 300` (la ligne d'arrivée) en découle. Vérifié par calcul direct (Node) : **exactement 200 étoiles / 100 obstacles** générés sur toute la ligne. La période de grâce initiale (aucun obstacle les ~8 premières secondes) est préservée, traitée à part du calcul de quota. Le rythme d'apparition (`CADENCE`) et la rampe de difficulté (beaucoup d'étoiles tôt, de plus en plus d'obstacles tard) sont inchangés dans l'esprit — seule la MÉTHODE devient exacte plutôt qu'approximative.
- ✅ **Vitesse +20 %** (demandé explicitement, deux réglages distincts). `config.js` : `vitesseBase` 1,5 → 1,8 et `vitesseMax` 5,0 → 6,0 (+20 % chacun, "la vitesse globale"). `road.js` : `SPEED_DOUBLING_TIME` 68 → 57 s (+20 % de TAUX d'accélération = temps de doublement divisé par 1,2, "l'accélération").
- ✅ **Compression audio** (demandé explicitement, "compresse le mp3, regarde le webm"). Le MP3 servi était en 320 kbps CBR (10,3 Mo) — qualité "master", largement au-dessus de ce qu'un usage in-game justifie. Réencodé **depuis le WAV master** (jamais depuis le MP3 320k, pour ne pas cumuler deux passes de compression avec perte) en **128 kbps CBR → 4,1 Mo (-60 %)**, remplace maintenant le fichier servi (`assets/la-ville-est-belle.mp3` ; l'original 320k gardé à côté sous `la-ville-est-belle-320k-original.mp3` si besoin de revenir dessus). Écart de durée décodée vs `CONFIG.dureeMorceau` revérifié sous le seuil d'alerte existant (aucun warning console) — le réencodage n'a pas dérivé la durée.
  - **Essai WebM/Opus** (comme demandé) : 96 kbps → 3,3 Mo, ~19 % plus petit que le MP3 128k et sonne au moins aussi bien à ce débit (Opus est structurellement plus efficace que MP3). **Non retenu pour l'instant** : le support de `decodeAudioData` sur WebM/Opus est historiquement moins fiable sur Safari iOS que le MP3 (universellement supporté) — risqué sur la plateforme n°1 du projet (CLAUDE.md, "Safari iOS en priorité absolue") pour un gain supplémentaire modeste une fois le MP3 déjà allégé de 60 %. Fichier de test supprimé après mesure (régénérable en 30 s depuis le WAV master si le sujet revient).
- ⬜ **Rien de tout ça n'a été testé sur iPhone réel** ni redéployé sur Netlify — en attente de confirmation avant de redéployer (question crédits encore fraîche dans la conversation).

**Reste à faire (prochaine session) :** tout ce qui précède reste à valider sur iPhone réel puis à déployer. Attention particulière aux rangées de voitures (3 voitures sur 4 voies ne laisse qu'UNE voie de passage — à confirmer que ça reste juste, pas injuste) et à la difficulté globale (200 étoiles à collecter + voitures tripées + vitesse +20 % change beaucoup d'un coup : possible qu'il faille redescendre un curseur après le premier vrai playtest).

**Refonte visuelle des écrans hors-jeu (maquette fournie par l'artiste, style "borne arcade claire") :**
- 🎨 **Maquette reçue** : capture d'écran + composant React/Tailwind de référence pour l'étape "Ton pseudo" — carte blanche à bordure noire 2 px, bandeau d'en-tête rouge plat à bord noir, champs/texte en noir, bouton "Suivant →" à bordure noire (grisé/bordure grise quand désactivé), pastilles d'étape (pilule rouge active + point gris). **Le composant React n'a pas été repris tel quel** (interdit par `CLAUDE.md` — vanilla JS + Canvas 2D uniquement) : le design a été retranscrit dans le système `.panel`/`.panel-header`/`.btn` déjà en place dans `index.html`, réutilisé par les 3 écrans hors-jeu (onboarding, écran de fin, pause) plutôt que dupliqué.
- ✅ **Carte `.panel` repassée en clair** : fond blanc plein (`--panneau-fond`), bordure noire 2 px (`--panneau-bord`), bandeau `.panel-header` en aplat rouge (plus de dégradé) à bord bas noir. Tous les textes/valeurs à l'intérieur des cartes (score, classement, notice son, champs pseudo/insta, barre de chargement, lien "Recommencer la course") repassés en noir/gris foncé — étaient en blanc, pensés pour l'ancien fond sombre. Ce qui reste hors carte (titre du jeu, CTA "Écouter le morceau", boutons son/pause) garde son traitement blanc existant, adapté au fond de jeu qui reste visible derrière, pas à une page blanche comme sur la maquette isolée.
- ✅ **Bouton principal** (`.btn-primary`) : bordure noire quand actif (au lieu du rouge foncé précédent) ; état désactivé repassé en gris franc (`#ccc`/bordure `#bbb`) plutôt qu'un simple rouge en transparence — se distingue nettement de l'état actif, comme sur la maquette. Flèche ajoutée uniquement sur "Suivant" (`Suivant →`, étape 1/2 du menu) : c'est la seule action de type "étape suivante" du jeu, JOUER/REJOUER/REPRENDRE restent des verbes d'action sans flèche.
- ✅ **Pastilles d'étape ajoutées** sous la carte du menu (`#step-dots`, 2 points — une par étape de l'onboarding), synchronisées avec `showStep()` dans `main.js` : pilule rouge pour l'étape active, point pour l'autre. Couleur du point inactif volontairement laissée claire/translucide (pas noir 20 % comme sur la maquette) : il repose sur le fond de jeu, pas sur une page blanche.
- ✅ **Voile derrière les cartes allégé** (demande explicite : « fond transparent, opacité »)  — `#overlay` : dégradé de fond ~2× plus transparent qu'avant (0,1 → 0,3 → 0,5 au lieu de 0,3 → 0,7 → 0,92) ; `#pause-screen` : 0,82 → 0,45. La carte elle-même reste blanche et opaque (comme sur la maquette), c'est le voile qui l'entoure qui laisse maintenant beaucoup plus voir le jeu en cours derrière.
- ✅ Curseur de volume du menu pause (`#pause-volume-slider`) adapté au nouveau fond blanc : piste et bordure du curseur repassées en noir (étaient pensées pour un fond sombre) ; le curseur du panneau son en jeu (`#volume-slider`, fond sombre inchangé) n'est pas concerné.
- ✅ Vérifié en preview mobile (375×812, dark) : les 3 écrans (menu étape 1 "Ton pseudo", menu étape 2 "C'est parti", écran de fin avec classement, menu pause) affichent la carte blanche/bordure noire/bandeau rouge attendue, textes lisibles, pastilles d'étape qui basculent bien au clic sur "Suivant →", jeu visible en transparence derrière le voile allégé. Aucune erreur console.
- ⬜ Non testé sur iPhone réel (comme le reste des refontes visuelles précédentes).

**Session « rattrapage prod + filtre pause + cache + rangées de voitures » :**
- ✅ **Netlify remis à jour** — le commit "flou de mouvement" et toute la refonte "borne arcade claire" (session précédente) n'étaient jamais partis en prod (bundle en ligne encore `index-C0tAeoFX.js`, celui du 9 août). Commit du travail en attente + `npm run build` + `netlify deploy --prod` : prod maintenant sur `index-CK5XKabY.js`, vérifié via `curl`.
- ✅ **En-têtes de cache Netlify** (`public/_headers`, copié tel quel dans `dist/` par Vite) — répond à la question « on peut vider le cache automatiquement tous les jours ? ». Une purge quotidienne n'aurait rien réglé : le problème n'est pas le cache de Netlify (déjà à `max-age=0` par défaut sur `index.html`) mais celui de **Safari sur le téléphone du testeur**, qui retient agressivement une ancienne réponse tant que le serveur ne lui dit pas explicitement de revérifier — une purge côté Netlify n'a aucune prise sur un cache déjà stocké sur l'iPhone. Fichier créé avec des règles explicites par type : `index.html`/`config.js` (rien n'a de hash dans le nom) → `no-store` (toujours revalidés, donc plus jamais besoin d'un rechargement forcé) ; `assets/*.js` (bundle Vite, nom en `index-<hash>.js`, change à chaque build) → cache 1 an ; MP3/polices (noms fixes mais contenu quasi figé) → cache 1 semaine, pour ne pas retélécharger 4,1 Mo à chaque partie (c'est ce qui avait mangé des crédits Netlify, voir session précédente). Pas encore vérifié en tête HTTP après déploiement (à faire au prochain accès prod) — le fichier est bien copié dans `dist/_headers` par le build.
- ✅ **Filtre passe-bas sur le menu pause** (demandé explicitement : « qu'on entende que les basses avec fondu »). `audio.js` largement réécrit : `pause()`/`resume()` remplacés par un unique `setPlaybackMode("running" | "muffled" | "silent")` — trois régimes plutôt que deux, parce que le menu pause et la perte de focus ne veulent PAS la même chose du son (menu pause = le morceau continue, étouffé ; onglet quitté = silence complet, personne n'écoute). Nouveau `BiquadFilterNode` (`lowpass`) inséré dans la chaîne entre `volumeGain` et `focusGain`, fermé à `CONFIG.pauseFiltreHz` (800 Hz) en mode `muffled`, grand ouvert (20 kHz, inaudible) sinon — rampe **exponentielle** (`exponentialRampToValueAtTime`) plutôt que linéaire, une fréquence de coupure se perçoit en octaves : une rampe linéaire semblerait ne rien faire pendant la majeure partie du fondu puis tout faire à la fin. Durée du fondu dans `config.js` (`pauseFondu`, 0,5 s), pas une constante moteur.
  - **Piège résolu — le morceau ne doit pas dérailler pendant la pause.** La course gèle (l'horloge de jeu doit rester figée, comme avant), mais le `BiquadFilterNode` a besoin d'un `AudioContext` qui **tourne** pour appliquer le filtre — donc plus question de `audioCtx.suspend()` en mode `muffled` (ça gèlerait le son en l'état, pas en version étouffée). Le morceau continue donc réellement de jouer en arrière-plan pendant tout le menu pause. Nouvel ancrage `muffleAnchor` : `now()` renvoie cette valeur gelée tant qu'elle est posée (la course ne voit rien passer), et à la sortie, `playNow(muffleAnchor)` relance une nouvelle source **depuis cet instant précis** — le petit bout de morceau joué pendant la pause est donc rejoué (pas sauté), pour que la grille rythmique (bonus/obstacles calés sur les beats) reste calée sur l'audio à la reprise.
  - `main.js` : les deux drapeaux existants (`manualPaused`, `hiddenPaused`) se combinent maintenant en un mode unique (`hiddenPaused` gagne toujours — inutile de jouer étouffé pour un écran que personne ne regarde) via `applyPauseState()`, simplifiée pour ne plus avoir à traquer les transitions à la main (`audio.setPlaybackMode()` ignore déjà un mode identique au courant).
  - **Vérifié en important le vrai module du jeu depuis la console** (technique documentée dans ce fichier, § pièges) : `audio.now()` strictement identique à trois lectures espacées de 1,4 s pendant le mode `muffled` (course bien gelée) ; à la sortie, l'horloge repart exactement de l'instant gelé et avance normalement (+0,70 s après 0,7 s d'attente réelle) ; `getStatus()` affiche `"lecture (pause, filtre 800 Hz)"` pendant la pause. Cycle complet rejoué via l'UI réelle (bouton Pause → panneau affiché, `temps=` figé à l'écran pendant 2 s d'attente → Reprendre → `temps=` repart, `horloge=audio` conservé, aucune bascule sur l'horloge de secours, aucune erreur console).
  - ⬜ **Non vérifiable dans cet environnement** : le rendu sonore réel du filtre (est-ce que 800 Hz "sonne juste" — trop étouffé/pas assez ?) ne peut s'apprécier qu'à l'oreille, sur device. Le réglage vit dans `config.js` (`pauseFiltreHz`) si un ajustement est nécessaire après écoute.
- ✅ **Rangées de voitures sous rampe de difficulté** (`entities.js`). Le point resté ouvert la session précédente (« 3 voitures sur 4 voies ne laisse qu'UNE voie de passage — à confirmer que ça reste juste, pas injuste », jamais confirmé par un vrai playtest) est traité par défaut, faute de retour de terrain : la probabilité d'une rangée de 3 voitures n'est plus une constante (15 % dès le premier obstacle rencontré) mais suit la même rampe `t` (progression du parcours, 0 en sortie de période de grâce → 1 à la ligne d'arrivée) que `BONUS_RATIO_START/END` juste au-dessus dans le fichier. En tout début de course : 0 % de rangée à 3 (au moins 2 voies toujours libres, le temps de prendre en main les 4 voies) ; ça monte ensuite progressivement jusqu'à 15 % pile à la fin, comme avant. Vérifié par calcul direct (Node, formule extraite telle quelle) : 0 % au slot 0, 2 % au slot 50, 10 % au slot 200, 15 % au slot 299 (dernier).
- ✅ **Déployé en production** après vérification en preview (`npm run build` sans erreur, module chargé sans erreur console avec `?debug`). Prod remise à jour avec l'ensemble de cette session.
- 💬 **Concours toujours ouvert publiquement** (`dateOuverture` toujours au 5 août) — confirmé sans risque par l'artiste (« j'ai ouvert à personne encore ») : pas de changement fait, le rappel dans `config.js` (repasser au 17 août avant tout partage du lien) reste valable pour plus tard.
- ⬜ **Rien de tout ça n'a été testé sur iPhone réel** — en particulier le rendu du filtre passe-bas à l'oreille (point demandé explicitement, jugeable uniquement sur device) et la nouvelle courbe de difficulté des rangées de voitures.

**Reste à faire (prochaine session) :** écouter le filtre passe-bas sur iPhone réel et ajuster `pauseFiltreHz`/`pauseFondu` si besoin ; confirmer que les en-têtes de cache éliminent bien le besoin de rechargement forcé après un déploiement (vérifier les headers HTTP en prod) ; confirmer que la nouvelle rampe de difficulté des voitures se sent progressive et pas trop timide en fin de parcours ; le reste en attente de longue date (bâtiments 3D ✅ déjà fait, pause hors focus ✅ déjà fait — restent : DA claire sur device réel, ligne d'arrivée visuelle jamais tranchée, étape 8 perf/livraison).

**1er playtest réel avec un joueur externe (Antoine) — grosse session de retours groupés :**
- 🐛 **Bug trouvé et corrigé — « pop-in » du tout premier lot d'objets.** Signalé : « animation bizarre au tout début, les étoiles apparaissent et disparaissent, comme si tout était déjà chargé ». Cause réelle : `clock.now()` vaut exactement 0 à l'instant où `gameStarted` passe à vrai, or le créneau 0 (le tout premier temps du morceau, donc de la période de grâce) est censé arriver PILE à cet instant — sans décalage, tout le premier lot de créneaux se retrouvait donc déjà À la position du joueur dès la toute première frame, au lieu d'avoir glissé depuis l'horizon comme n'importe quel créneau plus tard en course (ceux-là n'ont simplement jamais eu de frame précédente pour approcher progressivement). ✅ Corrigé (`entities.js` : nouvelle constante `LEAD_IN = LOOKAHEAD_SLOTS × CADENCE × beatPeriod` ≈ 7,5 s ; `main.js` : `clock.jumpBy(-entities.LEAD_IN)` juste après avoir calé l'horloge sur l'audio, au premier départ ET au rejeu) — le créneau 0 démarre alors exactement à la limite de visibilité (comme tout créneau qui vient d'apparaître à `LOOKAHEAD_SLOTS` d'avance) et glisse normalement jusqu'au joueur. Vérifié en preview : `temps=-7,48s` au tout début de course, une seule étoile visible pile à l'horizon (pas un paquet groupé sur le joueur), défilement fluide ensuite jusqu'au ramassage.
- 🐛 **Pseudo sans Insta = score orphelin.** Un joueur a rempli son pseudo mais pas son Insta ; comme le pseudo affiché au classement est un texte libre, PMC n'a aucun moyen de le retrouver pour le contacter s'il gagne. ✅ Les deux champs sont désormais **obligatoires** pour débloquer "Suivant" (`main.js`, `syncPseudoStep()` — l'écouteur `insta-input` appelait déjà `localStorage.setItem` mais jamais `syncPseudoStep()`, donc le bouton ne réagissait qu'au pseudo). Placeholder et texte d'aide mis à jour (`index.html`) pour annoncer clairement que l'Insta est obligatoire. Vérifié : bouton "Suivant" reste désactivé pseudo seul, se débloque une fois les deux remplis.
- ✅ **Décompte 15 s → 20 s**, repositionné en HAUT de l'écran (au lieu de centré au milieu, en concurrence avec le personnage) et agrandi (`#countdown-num` 88px→104px). Légendes agrandies (18px→22px) et enrichies d'icônes/flèches (🏆, ← → ↑, ★) plutôt que du texte seul — demandé explicitement après le test. `COUNTDOWN_START` dans `main.js`, CSS dans `index.html` (`#overlay.countdown-view` passé en `justify-content: flex-start`). Vérifié en preview : "20" bien en haut, personnage toujours visible en dessous, aucune régression du tutoriel jouable pendant le décompte.
- 🐛 **Bug corrigé — cœur qui reste affiché/clignote sur l'écran Game Over.** Cause exacte non reproduite en preview (tout le code tracé semblait déjà cohérent : `game.lives` retombe bien à 0 avant l'affichage), donc correctif **défensif** plutôt qu'un unique point de bascule : `hud.js` force maintenant l'affichage à 0 cœur dès que `game.ended && game.endReason === "gameover"`, indépendamment de la valeur exacte de `game.lives` ; `main.js` (`updateHealthFilterIfChanged`) fige le filtre canvas sur `.game-over-bw` via une sentinelle dès `game.ended`, sans plus jamais repasser par la comparaison de vies qui pouvait laisser rejouer `.heart-warning`. Vérifié en preview (`?debug`, touche G) : 3 cœurs vides, canvas bien en noir et blanc statique, aucun clignotement résiduel.
- ✅ **Clignotement du dernier cœur throttlé à toutes les 4 s** (`index.html`, `@keyframes canvas-heart-warning`) — retour direct : « très très bien, mais il faut qu'il apparaisse toutes les 4 secondes, sinon c'est beaucoup trop stressant et intense » sur la durée d'une vraie course. Passé d'un cycle continu de 1 s à un cycle de 4 s avec un bref aller-retour en tout début de cycle (reste ensuite en couleur ~3,4 s) — même principe (un flash N&B = rappel du danger), fréquence beaucoup plus respirable.
- 🐛 **Bug trouvé et corrigé — conflit piéton/étoile dans la même voie.** Signalé : « y'a des conflits personne et étoiles, ça doit jamais arriver ». Le piéton est un « mur humain plein », infranchissable même en sautant : une étoile apparaissant dans la MÊME voie à un créneau tout proche forçait un choix injuste (la rater ou risquer la collision), un piège que rien d'autre dans le jeu n'impose. ✅ `entities.js` : `slotContent()` scindé en `rawSlotContent()` (tirage brut, consulté par les voisins — jamais par lui-même, pour ne pas boucler à l'infini si deux créneaux voisins tombent tous les deux sur "piéton") + une vérification qui retire le piéton du tirage (remplacé par voiture/cône, tous deux franchissables au saut) dès qu'un voisin immédiat (`PIETON_BONUS_GUARD_SLOTS = ±1`) est un bonus dans sa voie. Voiture/cône restent volontairement autorisés à partager une voie avec un bonus proche : sauter les évite sans sacrifier l'étoile. Vérifié : build sans erreur, partie jouée sans blocage ni ralentissement (pas de récursion), aucune erreur console.
- ✅ **Écran de fin recentré** (retour confirmant un choix déjà en place : « le score en haut au centre c'est mieux, les cœurs à droite c'est mieux » — déjà le cas dans `hud.js`, rien à changer). Aucune action nécessaire, juste vérifié que c'est bien ce qui est en ligne.
- ✅ **Police des chiffres/nombres vérifiée** (demande explicite de contrôle) — audit de tous les `font:`/`ctx.font` du projet (DOM et canvas) : tout référence déjà `var(--police)`/`POLICE` (Stage Grotesk), y compris score, cœurs, décompte, classement, pourcentage de chargement. Rien à corriger.
- 🐛 **Bug trouvé au passage (non signalé, repéré en corrigeant "Voici votre score") :** `#score-label` était resté en blanc (`rgba(255,255,255,0.45)`), un reliquat de l'ancien fond sombre d'avant la refonte "borne arcade claire" — invisible sur le panneau clair actuel. Corrigé en noir.
- 💬 **Confusion 20 s décompte vs. barre de chargement** — le test a mêlé deux écrans différents (le chargement du MP3, 0→100 %, durée variable selon le réseau ; puis, une fois JOUER pressé, le décompte fixe de 20 s). Le décompte ne peut pas être sauté (pas de bouton "Passer", aucun raccourci clavier actif sur cette vue) — vérifié dans le code, rien à corriger, mais à re-expliciter clairement à l'artiste : les deux écrans sont bien distincts.
- ✅ **Netlify remis à jour** avec l'ensemble de cette session.
- 💬 **Plusieurs demandes plus lourdes, pas encore implémentées** (nouvelle DA cycliste d'après référence, glissade rapide vers le sol au relâché du saut, dégradation du son en 128 kbps sur le dernier cœur, événement panneau "Attention" avec rangée de 4 voitures bloquant toutes les voies vers 1 min 30, obstacle "pont" récurrent toutes les 35-40 s à une seule voie de passage) — voir le plan détaillé donné en réponse à l'artiste, en attente de confirmation/priorisation avant de s'y attaquer. Référence "pont" jamais reçue malgré l'annonce.
- ⬜ **Rien de tout ça n'a encore été reconfirmé sur iPhone réel** — c'est le prochain test.

**Reste à faire (prochaine session) :** retester avec Antoine (ou un autre joueur externe) tous les points ci-dessus, en particulier le pop-in corrigé et le clignotement throttlé (les deux points les plus visibles au précédent test) ; obtenir l'image de référence du pont ; prioriser avec l'artiste parmi les 5 chantiers lourds proposés (voir plan détaillé juste en dessous) avant de commencer l'un d'eux.

**Mise en ligne — le jeu est déployé et testable en vrai :**
- ✅ **https://pmc-la-ville-est-belle.netlify.app** — c'est l'URL de playtest, remise à jour à chaque vague de correctifs de la session. Le dépôt n'a **aucun remote git** : le déploiement ne passe donc pas par une intégration Netlify↔GitHub mais par le CLI, depuis le dossier du projet :
  ```
  npm run build && npx netlify deploy --prod --dir=dist
  ```
  Site déjà lié (`ea72e3f5-4d7a-40e2-bf5e-d96daa72d23c`, compte Paul MATHIEU COLLIN), rien à reconfigurer.

**Playtest en direct sur l'URL Netlify — 3 vagues de retours enchaînées (branche `gameplay-pause-voitures-etoiles`) :**

*Vague 1 — le démarrage (2 correctifs successifs sur la même cause) :*
- 🐛 **« Y'a un vrai problème de démarrage, y'a des gens qui reculent. »** Le `LEAD_IN` de la session précédente n'avait corrigé qu'une moitié du problème. La vraie cause, restée invisible jusqu'ici : `road.js` avait une **rampe de démarrage** (smoothstep 0 → vitesse de base sur 4 s) appliquée à `currentSpeed`. Or les entités n'ont pas de position stockée — `entities.js` recalcule leur profondeur **chaque frame** avec `z = PLAYER_NEAR_Z + deltaT × currentSpeed`. Une vitesse qui part de 0 écrase donc *tous* les `z` sur la position du joueur (pop-in massif), puis les repousse vers l'horizon quand elle remonte — d'où des objets qui « reculent ». Simulé en Node avant/après pour confirmer : l'ancienne courbe tombait de 19,8 à 0 en 1 s, la nouvelle reste plate à 19,8.
- ✅ **1re itération** (`9c7a7c3`) : rampe découplée, appliquée seulement à `distanceScrolled` (défilement visuel de la route), plus à `currentSpeed`. Le pop-in disparaît — mais nouveau retour : « il y a que des étoiles qui apparaissent et c'est super bizarre », la route ne bougeait plus pendant le lead-in.
- ✅ **2e itération, retenue** (`eb9e608`) : **rampe supprimée entièrement**. La route défile à `vitesseBase` dès la première frame. L'accélération exponentielle du morceau (`vitesseBase` → `vitesseMax` sur 257 s, `SPEED_DOUBLING_TIME`) suffit largement à faire monter la difficulté ; la rampe n'apportait qu'une sensation de « jeu qui ne démarre pas ». `START_RAMP_DURATION` retirée de `road.js`.

*Vague 2 — le premier tiers de course (`94bdcca`) :*
- ✅ **Période de grâce 8 s → 2 s** (`GRACE_BEATS` 16 → 4). Retour : « il y a juste plein d'étoiles, il se passe rien — je veux des objets exactement comme le reste du circuit ». Le début de course propose maintenant le même mélange étoiles / voitures / piétons que partout ailleurs, juste le temps de voir la route avant le premier danger.
- ✅ **Roues latérales des voitures retirées** (« c'est très moche »). Les phares/le halo ajoutés à la vague précédente restent.
- 🐛 **Cyclistes NPC qui passaient par-dessus les étoiles.** L'anti-chevauchement existait déjà mais prenait sa décision **une seule fois**, à la première apparition du cycliste, et la mettait en cache (`npcVisibilityDecision`). Ça ne pouvait pas marcher : les NPC approchent à `speed + NPC_CLOSING_EXTRA` (7 u/s de plus que les entités), donc un cycliste « libre » au moment du spawn rattrape et traverse une étoile plus tard. ✅ Cache supprimé, le test tourne à chaque frame — le cycliste (purement décoratif, il ne perd jamais contre le gameplay) s'efface tant qu'il chevauche une étoile ou une voiture, au lieu de passer au travers.

*Vague 3 — 3 retours reçus, diagnostic fait, **AUCUN correctif écrit** (session interrompue là) :*
- 🐛 **La musique semble revenir en arrière à la reprise de pause.** Retour exact : « quand on met reprendre, il reprend au moment où on a mis pause au niveau de la musique. Il faudrait juste une surcouche de filtre : le morceau doit continuer, le filtre passe de 100 % à 0 %. » — **Diagnostic établi, la cause est explicite dans le code** (`audio.js`, `setPlaybackMode`) : en mode `muffled`, le morceau continue mais l'horloge de jeu est gelée à la main (`muffleAnchor`), parce que `audioCtx.currentTime` avance toujours et que la course, elle, doit rester figée. À la reprise, l'écart accumulé est rattrapé en **relançant la lecture à l'instant gelé** (`playNow(muffleAnchor)`) — c'est exactement le saut en arrière entendu. Le commentaire du fichier l'assume comme « le prix à payer pour que bonus et obstacles retombent sur les temps ».
  **Ce qui est demandé revient à trancher l'arbitrage dans l'autre sens** : ne plus toucher à la lecture audio, et resynchroniser la *course* sur le morceau à la reprise (décaler l'origine de l'horloge de jeu de la durée de la pause plutôt que de rembobiner le son). À vérifier au passage : `LEAD_IN`, `resolved`/`consumed` (les créneaux « traversés » pendant la pause ne doivent pas arriver d'un bloc sur le joueur) et le chien de garde `audioWatch`.
- 🐛 **Classement absent de l'écran de fin.** Retour : « il y a marqué "Voici votre score : 10 100" et je n'ai pas le tableau des meilleurs scores ni mon classement ». Le code est en place et n'a pas été touché (`#leaderboard` dans `index.html`, `renderLeaderboard()` + l'`await net.getTopScores()` dans `endGame()`, `main.js`) ; `#leaderboard` part avec `.hidden` et n'est démasqué que si la requête ramène au moins une ligne. **Piste n°1 à vérifier avant de toucher au code : la requête Supabase elle-même** (`net.js` renvoie `[]` en silence sur toute erreur — vue `scores_public` absente, RLS, réseau) ; `apiScores`/`apiScoresKey` sont bien renseignés dans `config.js`. À reproduire en prod avec la console réseau, pas en preview.
- 🐛 **« Je passe à travers des cyclistes sans perdre de points, parfois. »** — **Ce n'est pas un bug : les cyclistes NPC sont décoratifs par conception** (`entities.js`, section « Cyclistes NPC en sens inverse » : aucune collision, aucun `resolved`/`consumed`, générateur totalement séparé). Ils ressemblent pourtant à des obstacles et arrivent en sens inverse, donc l'attente du joueur est légitime. **Décision produit à prendre avec l'artiste** — soit ils deviennent de vrais obstacles (et il faut alors les faire entrer dans la grille de quotas `TOTAL_OBSTACLES`, sinon la difficulté du parcours change en douce), soit ils restent décoratifs et il faut les rendre visuellement inoffensifs (éloignés sur les voies extérieures, plus petits, plus transparents).

**Cyclistes promus en obstacles + nouvelle DA voxel (décision produit tranchée par l'artiste) :**
- ✅ **Les cyclistes sont de VRAIS OBSTACLES.** La question laissée ouverte la session précédente (« décoratifs ou dangereux ? ») est tranchée : dangereux. Plutôt que d'ajouter une collision au générateur NPC séparé, celui-ci est **entièrement supprimé** (~82 lignes) et le cycliste devient un **4e type sur la grille rythmique principale**, à côté de voiture/piéton/cône. C'est ce qui lui donne d'un coup les trois propriétés qu'un obstacle doit avoir et qu'un décor n'avait pas besoin d'avoir : anti-chevauchement gratuit (un seul objet par créneau, donc plus jamais de cycliste qui traverse une étoile), quota exact préservé, et surtout un timing **prévisible** — l'ancien générateur approchait à `speed + 7` u/s, une vitesse différente du reste, ce qui rendait tout obstacle bâti dessus injuste par construction. `CLAUDE.md` mis à jour : la décision verrouillée passe de 3 à 4 obstacles.
- ✅ **Comportement** : le cycliste rejoint le piéton dans `UNJUMPABLE_KINDS` — il se contourne latéralement, le saut ne sauve pas (contrairement à la voiture et au cône). Coût : −1 vie, comme piéton/cône (seule la voiture reste un choc fatal).
- 🐛 **Bug de conception trouvé en mesurant, invisible autrement.** La garde anti-piège (« pas d'obstacle infranchissable dans la voie d'un bonus voisin ») remplaçait le type de l'obstacle par une voiture/un cône. Réglée à l'époque pour le seul piéton (~21 créneaux sur 100), elle s'est mise à couvrir 39 créneaux dès l'arrivée du cycliste et à en **convertir 51 %** : sur 18 cyclistes tirés, 8 seulement survivaient, le premier n'apparaissant qu'à 67 s de course. La catégorie se vidait toute seule, en silence. ✅ Corrigé en résolvant le conflit là où il est vraiment — dans l'espace des **voies** : la garde DÉPLACE maintenant l'obstacle vers une voie libre (`laneOverride`, honoré par `slotLanes`) au lieu de changer son type. Le piège disparaît toujours, mais la distribution des types redevient celle qu'annoncent les poids. Vérifié par recensement direct sur les 300 créneaux : **200 étoiles / 100 obstacles** (quota intact), répartition voiture 46 / piéton 21 / cycliste 18 / cône 15, avec une montée en présence par tiers de parcours (3 → 8 → 7 cyclistes).
- ⚠️ **Effet de bord à surveiller au playtest** : en cessant de convertir, on garde ~39 obstacles infranchissables par partie au lieu de ~19. Le nombre total d'obstacles n'a pas bougé (100), mais la proportion qui ne se franchit QUE latéralement double. À confirmer en jouant — si c'est trop dur, le levier est le poids `cycliste` dans `OBSTACLE_WEIGHTS`.
- ✅ **DA refaite en voxel** (`cyclists.js` réécrit) d'après deux références fournies par l'artiste (rendus voxel/LEGO : barbu casquette blanche/sac rouge, et bandeau blanc/roues bleues). Canvas 2D ne peut pas importer de vrai voxel 3D (verrouillé dans `CLAUDE.md`), donc on en reprend les **signaux** : tout est construit en blocs via un primitif `blk()` qui pose une arête haute éclaircie et des arêtes basse/droite assombries (c'est ce seul détail qui fait lire « cube extrudé » plutôt que « rectangle de couleur »), roues épaisses à **crampons en quinconce**, sac à dos, casquette et barbe. **5 variantes** au lieu de 4. Ils sont désormais dessinés **de face** — ils roulent vers le joueur, et ils reprenaient jusqu'ici la géométrie de `player.js`, dessinée de dos.
- 🐛 **Bug attrapé au passage** : la fonction d'ombrage recevait parfois une couleur déjà convertie en `rgb(...)` et la traitait comme du hex → `parseInt` NaN → `fillStyle` invalide **silencieusement ignoré par Canvas**, donc un bloc peint avec la couleur précédente. Corrigé (`parseColor` accepte les deux formats).
- **Vérifié** : sprites contrôlés à l'échelle réelle du jeu (110 px au plus près jusqu'à 24 px au loin — la silhouette et les variantes tiennent) ; collision testée dans les trois cas (même voie → touché, autre voie → rien, **en l'air même voie → touché quand même**, c'est bien infranchissable) ; 658 frames rendues sur toute la course sans exception ; balayage des 4 voies → 64 à 78 objets rencontrés par voie sur 300 (attendu ~75), les 4 types se déclenchent.
- ⚠️ **Piège de test à retenir** (m'a fait croire à un bug inexistant) : importer un module avec un cache-buster (`/src/clock.js?v=123`) en crée une **seconde instance**, distincte de celle qu'utilisent les autres modules. Un `clock.setTimeSource()` posé dessus ne pilote alors rien, l'horloge du jeu reste à 0 et la simulation ne détecte qu'un seul objet. Importer sans query pour partager l'instance.

**Poids et structure du code — audit chiffré (question de l'artiste : « c'est optimisé ? ») :**
- 📊 **Le poids n'est pas un sujet.** Bundle JS **17 Ko gzippé** (45 Ko brut) contre **3,9 Mo de MP3** : l'audio pèse **99,6 %** du total. Zéro dépendance runtime (Vite est en devDependency). Optimiser la structure JS pour le poids serait du travail pur pour un gain non mesurable. Le MP3 est déjà à 128 kbps pour 4 min 18 — descendre plus bas dégraderait exactement ce que le jeu existe pour vendre. **Rien à faire.**
- 📊 **Le coût en tokens est ailleurs que dans les commentaires.** Densité de commentaires 30-40 % par fichier, mais ils encodent le POURQUOI de chaque décision de playtest — les couper ferait réintroduire des bugs déjà corrigés. Les deux vrais coûts fixes : `main.js` (1242 lignes, module fourre-tout : pause, DOM, onboarding, classement, physique du saut, boucle, collisions, fin de partie) et **ce fichier** (833 lignes, que `CLAUDE.md` impose de lire en entier à chaque session).
- ⬜ **Refactor `main.js` proposé et volontairement NON fait** : le mélanger à un changement de gameplay est le meilleur moyen de casser les deux. À faire comme passe séparée, quand aucun chantier de jeu n'est ouvert.
- 💬 Sur les outils : le playbook classique d'optimisation web (tree-shaking, code splitting, régime de dépendances) n'a rien à mordre ici — il n'y a pas de dépendances. Le skill `web-perf` reste pertinent pour le **runtime** (fps, coût de rendu canvas), pas pour le poids.

---

## PLAN — 5 chantiers lourds demandés après le 1er playtest externe (session suivante, nouvelle conversation)

**Mise à jour : le chantier 2 (glissade swipe bas) est FAIT** — voir ci-dessous. Les quatre autres sont toujours à l'état zéro. Contexte complet ci-dessous pour qu'une nouvelle session (autre modèle) puisse attaquer directement sans repasser par l'historique. Ordre de priorité non tranché — demander à l'artiste avant de commencer (question posée en fin de session précédente, jamais répondue : cyclisme d'abord / gameplay d'abord / tout en même temps sans repasser par lui entre chaque étape).

### 1. Nouvelle DA du cycliste, d'après une image de référence
**Demande exacte :** « Je te donne une image pour les cyclistes. Je veux que tu les réalises comme ça, parce que là, tu as fait des copies de moi en cyclisme. »

**La référence** (image fournie dans le chat, pas encore enregistrée dans le repo — **à redemander à l'artiste au début de la prochaine session**, ou décrite ici de mémoire en attendant) : un rendu **voxel/low-poly isométrique 3D** (style MagicaVoxel/Blockbench, PAS du pixel art 2D plat) — homme barbu (barbe rousse/brune), casque rose voxel, débardeur beige/tan, short marron, baskets rouges, sac à dos rose et blanc en damier, assis sur un vélo à cadre blanc avec de **très grandes roues** (rayons blancs, jante marron), le tout posé sur une petite plateforme de briques beiges.

**⚠️ Contrainte technique à trancher avant de commencer :** `CLAUDE.md` verrouille **vanilla JS + Canvas 2D, aucun moteur 3D**. La référence est un vrai rendu 3D volumétrique — elle ne peut pas être reproduite telle quelle au runtime avec la technique actuelle (`player.js` : sprite pixel art 2D pré-rendu une fois au chargement, blit à l'échelle, exactement comme `pedestrians.js`/`cyclists.js`). Deux options, à faire trancher par l'artiste :
  - **(a) Réinterprétation pixel art** — redessiner un sprite 2D en s'inspirant de la référence (grandes roues, sac à dos, casque, barbe, palette) mais avec la même technique procédurale que l'existant (rectangles/cercles Canvas, comme `player.js` actuel). Le plus rapide, cohérent avec le reste du pipeline, mais un pixel art plat ne rendra jamais un rendu voxel 3D à l'identique — juste "dans le même esprit".
  - **(b) Vrais rendus voxel importés** — faire produire (par l'artiste ou en dehors du jeu) des sprites voxel pré-rendus (rotations/frames de pédalage) en PNG, importés comme images statiques à la place du dessin procédural. Beaucoup plus fidèle à la référence, mais change le pipeline (asset externe au lieu de tout générer en Canvas) et demande un aller-retour avec l'artiste pour les fichiers.
  - **Recommandation : (a) d'abord**, à valider par l'artiste avant de s'engager — c'est réversible et rapide à montrer, (b) reste possible ensuite si le résultat ne satisfait pas.

**Fichiers concernés :** `src/player.js` (silhouette/palette/roue/pédalage — la mécanique de lean/rebond ne change pas, seule l'apparence), `src/cyclists.js` (cyclistes NPC en sens inverse — réutilisent EXPLICITEMENT la géométrie de `player.js` "pour zéro risque visuel", donc à resynchroniser en même temps, pas séparément).

### 2. ✅ FAIT — Glissade rapide vers le sol (swipe vers le bas pendant le saut)
**Livré** (`input.js` : `triggerSlam()`/`consumeSlamDown()`, symétrique du swipe haut, plus `ArrowDown`/`KeyS` au clavier pour le confort de test ; `main.js` : le slam n'a d'effet que si `jump.mode === 'air'` et pousse `jump.vy` à `-vJump × 1.5`, donc la parabole existante est court-circuitée sans être remplacée par un cas particulier). La détection réutilise le même verrou « un cran par contact » que les autres gestes. Descriptif d'origine conservé ci-dessous pour mémoire.

**Demande exacte :** « Plusieurs choses quand tu swipes vers le haut, comme sur [Subway Surfers] : la possibilité de swiper vers le bas pour redescendre très rapidement vers le sol. » Contexte : le joueur remonte au clavier/tactile via un swipe vers le haut mais ne peut pas actuellement écourter la descente — « ça me ferait chier qu'on ne puisse pas redescendre une fois qu'on a sauté ».

**Approche technique :** `src/input.js` a déjà la détection du swipe vers le haut (déclenche le saut) — ajouter la détection symétrique du swipe vers le bas, mais seulement exploitable **pendant que `jump.mode === 'air'`** (voir `main.js`, la machine à états du saut : `ground`/`air`/`onCar`). Actuellement la hauteur suit une parabole physique (`jumpPhysics`, voir commentaire dans `config.js` sur `dureeSaut`/`hauteurSaut`) — la glissade rapide doit court-circuiter cette parabole en cours de vol (accélération vers le bas nettement plus forte, ou un `jump.y` qui redescend directement à 0 sur une durée courte fixe) dès l'input détecté, sans attendre la fin naturelle de l'arc. Attention à ne pas casser la détection de collision/ramassage aérien (étoiles aériennes, `AIR_BONUS_KINDS`) si l'atterrissage devient possible plus tôt que prévu.

**Fichiers concernés :** `src/input.js` (détection swipe bas), `src/main.js` (état du saut, `jump.*`).

### 3. Dégradation du son sur le dernier cœur (effet "radio pixel")
**Demande exacte :** « Si tu arrives, quand la personne a un cœur, à dégrader le son pour que le son soit avec un débit beaucoup plus léger, genre 128 kbps voire moins, pour qu'on ait cet effet pixel radio, ça serait super. » — à faire en même temps que le clignotement N&B déjà en place (§ session précédente), pas à sa place.

**⚠️ Nuance technique à connaître :** on ne peut pas "changer le bitrate" d'un flux Web Audio déjà décodé — le bitrate est une propriété de l'ENCODAGE du fichier source, pas un paramètre live. Ce qui EST possible en direct pour approcher l'effet "radio/téléphone" décrit : (a) un filtre passe-bande serré (ex. 300 Hz–3 kHz, coupe les graves ET les aigus — contrairement au simple passe-bas déjà utilisé pour le menu pause), (b) une réduction d'échantillonnage/bitcrush (`WaveShaperNode` ou `AudioWorklet`, plus lourd à mettre en place), (c) un léger bruit/grain ajouté en mixage. Le projet a déjà un `BiquadFilterNode` de passe-bas dans `audio.js` (`setPlaybackMode`, mode `muffled` du menu pause) — s'en inspirer pour le graphe, mais ce doit être une chaîne d'effet **séparée** (le menu pause et "dernier cœur en danger" sont deux états indépendants qui peuvent en théorie se chevaucher).

**Cadence à trancher :** le clignotement visuel vient tout juste d'être throttlé de continu (1s) à un flash toutes les 4s parce que le continu était "beaucoup trop stressant". Recommandation : caler la dégradation sonore sur la **même cadence de 4s** (pulsée, pas permanente) plutôt que de retomber dans le même piège d'intensité déjà identifié — à confirmer avec l'artiste plutôt qu'à décider seul.

**Fichiers concernés :** `src/audio.js` (nouveau nœud/chaîne d'effet, ex. `setDangerMode(active)`), `src/main.js` (déclenché au même endroit que `updateHealthFilterIfChanged`, idéalement sur la même horloge que le flash visuel pour que son et image pulsent ensemble).

### 4. Événement "ATTENTION" vers 1 min 30 — rangée de 4 voitures, saut obligatoire
**Demande exacte :** « À partir de une minute trente, j'aimerais que tu rajoutes des vrais problèmes, genre un panneau qui dit "Attention" en plein milieu de l'écran. D'un seul coup, il y a 4 voitures qui bloquent toutes les voies, et il est obligatoire de sauter pour passer. [...] ça c'est pour les gens qui sont vraiment très forts et qui arrivent à avancer. »

**Différence avec le système actuel :** les rangées de voitures existantes (`entities.js`, `CAR_ROW_EARLY`/`CAR_ROW_LATE`, voir plus haut dans ce fichier) plafonnent à 3 voitures sur 4 voies — **il reste toujours au moins une voie libre**, par construction. Ce nouvel événement demande l'inverse : **les 4 voies bloquées en même temps**, aucune échappatoire latérale, seul le saut (atterrissage sur le toit, mécanique déjà existante via `roofOverlap`/`renderCar3D`) permet de passer. C'est donc un événement **spécial, hors de la grille aléatoire habituelle**, pas un réglage de plus dans `CAR_ROW_SIZES`.

**Question ouverte à trancher avec l'artiste :** un moment unique et marquant dans toute la course, ou un motif qui revient plusieurs fois après 1 min 30 ? Le texte ("à partir de") suggère plutôt une réapparition possible, mais ce n'est pas explicite.

**Approche technique :** un créneau spécial calé sur un temps fixe (1 min 30 = 90 s → convertir en index de beat avec `clock.timeOfBeat`/`CADENCE`, comme le fait déjà `finishBeatN()`), qui force `pickLanes(slotIndex, 4)` (les 4 voies) au lieu du tirage pondéré habituel. Le panneau "ATTENTION" est un nouvel élément d'UI (bandeau plein écran ou gros texte canvas, à concevoir) qui doit apparaître quelques secondes AVANT que la rangée n'arrive à hauteur du joueur — même principe de timing que la légende du décompte ou l'`#end-note`, mais déclenché en cours de course.

**Fichiers concernés :** `src/entities.js` (créneau spécial + logique de blocage total), nouveau code de rendu pour le panneau "ATTENTION" (`hud.js` ou nouveau module), `src/main.js` (déclenchement/timing).

### 5. Obstacle "pont" récurrent (toutes les 35-40 s), une seule voie de passage
**Demande exacte :** « Ensuite, j'aimerais que tu rajoutes un pont. [...] il faut que les gens passent dans le pont et qu'ils aient une seule voie pour passer dans le pont. Faudrait que tu mettes ce pont toutes les 35-40 secondes, genre le pont Neuf qui traverse [Paris]. »

**🚧 BLOQUANT : référence jamais reçue.** L'artiste a annoncé « je vais te donner un exemple de design » mais aucune image n'est arrivée dans la conversation. **Ne rien dessiner avant de l'avoir reçue** — demander explicitement au début de la prochaine session.

**Ce qu'on sait déjà :** récurrent (~35-40 s, donc à convertir en un intervalle de beats fixe, cadence indépendante de la grille bonus/obstacle habituelle — comparable au système des cyclistes NPC décoratifs, `NPC_CADENCE`/`NPC_LOOKAHEAD`, qui tourne déjà sur sa propre horloge séparée), et une seule voie de passage pendant le franchissement (rétrécissement temporaire de la largeur praticable, ou piliers/parapets bloquant 3 des 4 voies). Techniquement plus proche d'un **volume architectural** (comme les bâtiments haussmanniens, `world.js`, projeté via `road.project()`) que d'un obstacle ponctuel classique — probablement une structure qui encadre la route plutôt qu'un objet dessus.

**Fichiers concernés :** vraisemblablement un nouveau module dédié (sur le modèle de `world.js`), plus une nouvelle règle de collision dans `entities.js` (contact avec les piliers/le parapet si le joueur n'est pas dans la voie centrale).

---

---

## 0. Concept figé

Un **runner à la Subway Surfers** : caméra placée **derrière le cycliste**, sol qui défile vers un point de fuite, façades haussmanniennes de part et d'autre. **Coucher de soleil parisien, 22 h** : ciel orangé, lumière chaude et forte rasant les façades.

- **Parcours fini** calé sur la durée du morceau (~257,9 s). Quand la chanson finit, la course finit → écran de score.
- **Pilotage continu libre** (pas de couloirs) : le joueur glisse latéralement en continu.
- **Contrôle principal : bande de pilotage tactile en bas de l'écran** pour diriger (⚠️ initialement gyroscope à l'inclinaison — revenu en arrière en session, voir « État d'avancement » : demandé explicitement après plusieurs sessions à fiabiliser la permission iOS). **Bouton tactile "saut"** pour sauter les obstacles au sol et ramasser les bonus aériens.
- **3 vies.** Une collision = -1 vie. À 0 vie, game over avant la fin du morceau.
- Le joueur ramasse des **bonus** (points) et évite/saute des **obstacles**.
- **Synchro musicale** : le monde pulse au tempo, les bonus/obstacles arrivent calés sur les temps. C'est l'effet "waouh" recherché.
- **Score + meilleur score.** Un **backend centralise tous les scores** (classement + dashboard).
- **Toujours jouable**, mais un **concours** avec dates d'ouverture/fermeture s'appose par-dessus (participation = score envoyé pendant la fenêtre).
- **Bouton partage** → génère une **image story 1080×1920 pixel art "borne arcade 80s"** (titre, score, lien).
- CTA "aller écouter" → **https://linktr.ee/pmc.mp3**

---

## 1. Données audio (mesurées sur le master fourni)

Fichier : `PMC - La ville est belle MASTER#3.wav` (44,1 kHz / stéréo / 24-bit).

| Donnée | Valeur | Note |
|---|---|---|
| **Durée** | **257,908 s** (4:17.9) | = durée du parcours |
| **BPM** | **120,0** | comb-filter net devant 80/160 |
| **Offset 1er temps** | **~0,01 s** | démarre quasi sur le downbeat |
| Période de beat | 0,5 s | ~516 temps, ~129 mesures en 4/4 |

**À faire côté artiste** : vérifier visuellement le calage avec la grille rythmique de debug (étape 2) et corriger `BPM`/`offset` dans `config.js` si besoin. Fournir un `.wav` **et** un `.mp3` (le `.mp3` de 10 Mo existe déjà) pour le web : servir le **MP3/AAC** en prod (poids), garder le WAV en interne.

**⚠️ iOS Safari** : l'`AudioContext` doit être débloqué par un **geste utilisateur** (le tap "Jouer"). Ne jamais tenter de démarrer l'audio automatiquement.

---

## 2. Choix techniques (et pourquoi)

| Choix | Décision | Justification mobile |
|---|---|---|
| Rendu | **Canvas 2D**, projection pseudo-3D (plan de sol + sprites scalés par profondeur) | Universel Safari/Chrome/Firefox, léger, économe en batterie, pas de risque driver GPU sur vieux iPhone. WebGL surdimensionné pour ce rendu. |
| Boucle de jeu | **Pas de temps fixe** (accumulateur) + interpolation au rendu | iPhone 60 **ou** 120 Hz (ProMotion), Android variable → physique et synchro déterministes. |
| Horloge maîtresse | **Web Audio API** (`AudioContext.currentTime`) | `<audio>` dérive/latence imprévisible sur iOS. Web Audio = calage précis sur la grille de beats. |
| Direction | **Bande de pilotage tactile** en bas d'écran (`#steer-control`) | ⚠️ Initialement gyroscope (`deviceorientation`) — retiré en session après plusieurs allers-retours sur la fiabilité de la permission iOS, remplacé par un contrôle tactile dédié et visible (demandé explicitement). Voir « État d'avancement ». |
| Orientation | **Portrait verrouillé**, plein écran, safe-areas iOS (`env(safe-area-inset-*)`) | Responsive tous écrans, jeu une main. |
| Build/dev | **Vite** (vanilla JS + Canvas, aucun framework lourd) | HMR, `server.host: true` pour LAN, build statique optimisé. |
| Front prod | Statique déployé sur **Vercel ou Netlify** (HTTPS d'office) | HTTPS résout le partage de fichier en prod. |
| Backend | **Supabase** (Postgres managé + API REST instantanée + dashboard) recommandé | Voir §6. Alternative : petit service Node + Postgres. |

**Pourquoi pas de framework JS (React…)** : coût de bundle et de runtime inutile pour un jeu Canvas ; on veut un démarrage rapide sur data mobile et un contrôle total de la boucle.

**Serveur de dev accessible sur le LAN (point 1 du brief)** :
- `vite --host` → `http://<ip-locale>:5173`, ouvrable depuis l'iPhone sur le même Wi-Fi.
- **MAIS** le Web Share (fichier, étape 7) exige un **contexte sécurisé**. En HTTP LAN il est bloqué. → Générer un **certificat local avec `mkcert`** et lancer Vite en HTTPS, **ou** passer par un tunnel (`cloudflared`/`ngrok`) pour tester cette brique sur mobile. Le reste (route, audio, gameplay, direction tactile) se teste très bien en HTTP LAN.

---

## 3. Architecture des fichiers

`✅` = existe déjà et fait ce qui est décrit. `⬜` = pas encore créé (étape à venir).

```
/
├── config.js            ✅ SEUL fichier de réglages, gelé (Object.freeze) — voir §4
├── index.html           ✅ canvas plein écran, viewport, safe-area, slider volume, bouton saut
├── vite.config.js       ✅ server.host:true (HTTPS mkcert : bloc prêt, commenté — voir §2)
├── .claude/launch.json  ✅ config du skill `run` (npm run dev, port 5173)
├── /src
│   ├── main.js          ✅ bootstrap, boucle fixed-timestep, geste de démarrage, état de partie, HUD/écran de fin
│   ├── clock.js         ✅ horloge de jeu, source de temps swappable (perf.now → audio)
│   ├── audio.js         ✅ Web Audio, unlock iOS, fondu in/out, volume
│   ├── road.js          ✅ moteur pseudo-3D scanline (plan de sol, profondeur, projection, caméra)
│   ├── world.js         ✅ immeubles nus qui défilent (parallaxe) + dégradé ciel coucher de soleil — façades détaillées restent à faire (étape 4)
│   ├── player.js        ✅ sprite cycliste (silhouette B), rendu, lean, rebond de pédalage
│   ├── entities.js      ✅ spawn bonus/obstacles calé sur les beats, collision/ramassage, icônes pixel art
│   ├── input.js         ✅ bande de pilotage tactile (#steer-control) + clavier + bouton saut — gyroscope retiré
│   ├── debug.js         ✅ mode debug (FPS, position, grille rythmique, statut gyro)
│   ├── hud.js           ✅ écrans hors-jeu : prompt de démarrage (titre + CTA), HUD, indicateur gyro, écrans de fin (score + statut concours)
│   ├── share.js         ⬜ étape 7 — génération image story 1080×1920 pixel art
│   └── net.js           ⬜ étape 7 — envoi score au backend + récup classement
├── /assets              ✅ la-ville-est-belle.mp3 + MASTER.wav (fournis)
└── /public/assets       ✅ symlink → ../../assets/la-ville-est-belle.mp3 (nécessaire pour Vite,
                             voir §2 "Serveur de dev") — n'expose PAS le WAV master
```

`config.js` n'est **jamais** modifié par la logique de jeu : chargé en premier, exposé en objet global `CONFIG` en lecture seule.

---

## 4. `config.js` — spécification complète (copier-coller de départ)

```js
// config.js — Réglages du jeu "La ville est belle".
// Fichier UNIQUE de configuration. Ne contient AUCUNE logique de jeu.
// Tout se règle ici sans toucher au moteur.

window.CONFIG = {

  // === MORCEAU / RYTHME ===
  bpm: 120,                 // Tempo mesuré sur le master (à affiner avec la grille de debug)
  premierTempsOffset: 0.01, // Décalage en secondes du 1er temps depuis le début du fichier
  dureeMorceau: 257.9,      // Durée du morceau en secondes = longueur du parcours
  fichierAudio: "assets/la-ville-est-belle.mp3", // MP3/AAC servi en prod (léger)
  fonduEntree: 1.2,         // Fondu à l'entrée, en secondes (ajouté étape 2)
  fonduSortie: 2.0,         // Fondu à la sortie (juste avant la fin du morceau), en secondes (ajouté étape 2)

  // === VITESSE / PILOTAGE ===
  vitesseBase: 1.0,         // Vitesse d'avancement de base (multiplicateur global)
  vitesseMax: 1.8,          // Vitesse max atteinte en fin de parcours (montée de difficulté)
  sensibiliteDirection: 1.0,// Sensibilité de la direction (gyroscope) : plus haut = plus réactif
  zoneMorteGyro: 3,         // Angle mort du gyroscope en degrés (évite le tremblement)
  agressiviteVirages: 0.7,  // Amplitude/nervosité du déport latéral en virage (0 = mou, 1 = sec)

  // === SAUT ===
  hauteurSaut: 1.0,         // Hauteur du saut (multiplicateur)
  dureeSaut: 0.45,          // Durée du saut en secondes

  // === VIES / ÉNERGIE ===
  viesDepart: 3,            // Nombre de vies au démarrage
  drainEnergie: 0.04,       // Vitesse de vidage de la jauge d'énergie par seconde
  energieParBonus: 0.15,    // Énergie rendue à chaque bonus ramassé (0..1)
  // Énergie à 0 = ralentissement (pas game over). Les vies gèrent la mort.

  // === BONUS (valeurs de score) ===
  bonus: {
    clementine:    50,      // Clémentine (commun)
    clavier:      100,      // Clavier
    sourire:      150,      // Sourire
    collierPerles:250,      // Collier de perles
    etoile:       500,      // Étoile (rare, gros points / multiplicateur)
  },

  // === OBSTACLES ===
  cadenceSpawnBeats: 2,     // Un événement (bonus/obstacle) tous les N temps (calage rythmique)

  // === CONCOURS ===
  dateOuverture:  "2026-09-11T10:00:00+02:00", // Ouverture du concours (heure de Paris)
  dateFermeture:  "2026-10-11T23:59:59+02:00", // Fermeture du concours (heure de Paris)
  // Hors fenêtre : le jeu reste jouable, mais le score n'est pas comptabilisé au classement.

  // === LIENS ===
  lienEP: "https://linktr.ee/pmc.mp3", // "Aller écouter le morceau"

  // === BACKEND ===
  apiScores: "",            // URL de l'endpoint d'envoi/lecture des scores (rempli à l'étape 7)

  // === DEBUG ===
  toucheDebug: "d",         // Touche clavier pour activer/désactiver le mode debug
};
```

*(Note design à confirmer avec l'artiste : "énergie" est interprétée ici comme une jauge d'allure qui se vide et que les bonus rechargent — à 0 on ralentit. Les **3 vies** restent le vrai game over. Ajuste si l'intention était différente.)*

---

## 5. Contenu de jeu (décisions)

**Bonus (5 types)** — sprites + valeurs dans `config.js` :
Clémentine · Clavier · Sourire · Collier de perles · Étoile.

**Obstacles (3 types, improvisés Paris + logique du saut)** :
1. **Portière de voiture qui s'ouvre** — surgit sur les côtés → à **éviter en se déportant**.
2. **Piéton distrait qui traverse** — → à **éviter en se déportant**.
3. **Trottinette abandonnée / nid-de-poule au sol** — → à **sauter** (donne un rôle clair au bouton saut).

**Mode debug (point 3 du brief)** — touche `CONFIG.toucheDebug` :
- FPS, position (x, z) du joueur, segment courant.
- **Grille rythmique en surimpression** sur la route (lignes qui tombent sur chaque temps/mesure) pour valider le calage sur le BPM à l'œil, sur mobile.

---

## 6. Backend (score centralisé + dashboard)

Demandé : « un énorme back-end pour centraliser toutes les réponses, score, dashboard ».

**Recommandation : Supabase** (Postgres hébergé, API REST/PostgREST instantanée, dashboard de tables intégré, Row Level Security). Le plus rapide à monter en sessions courtes.

- **Identifiant joueur = pseudo Instagram** (handle public, ex. `@pmc.mp3`). **Pas d'email.**
- Table `scores` : `id`, `pseudo_insta`, `score`, `created_at`, `game_version`.
- Endpoint d'écriture (POST score) + lecture (top N classement).
- **Dashboard** : la console Supabase suffit au départ ; une page interne de classement peut venir ensuite.

**Anti-triche : AUCUN.** Décision figée : on ne se protège pas contre les scores falsifiés. Le backend **garde tous les scores** ; la vérification pour le concours se fait **au screenshot** envoyé par le joueur. → POST score simple, pas de HMAC ni de validation de replay.

**RGPD (on est à Paris) :** un pseudo Insta public reste une donnée personnelle, mais l'exposition est minimale (pas d'email, pas de contact direct). Prévoir une courte mention (usage : classement du concours) et une politique de suppression sur demande. Léger.

---

## 7. Image de partage (point 5 du brief)

- Canvas hors-écran **1080×1920**, **pixel art**, rendu **côté client** (rien côté serveur).
- Style **écran de fin de borne d'arcade années 80** : fond sombre, gros pixels, palette néon, titre **"LA VILLE EST BELLE"**, **SCORE**, **HIGH SCORE**, et le **lien** (linktr.ee/pmc.mp3).
- Export via **`canvas.toBlob()` + Web Share API niveau 2** (`navigator.share({ files: [...] })`) — supporté par Safari iOS. **Repli** : téléchargement de l'image si le partage de fichier n'est pas dispo.
- C'est **l'objet qui circule en story** : la soigner autant que le jeu.

---

## 8. Découpage en étapes (avec critères de validation)

À la **fin de chaque étape** : livrer un état précis « fait / reste à faire » pour permettre d'arrêter et reprendre sans perdre le fil (point 6 du brief).

- ✅ **Étape 0 — Fondations** — FAIT.
  `config.js` + `index.html` (canvas plein écran, viewport, safe-area) + Vite LAN (+ HTTPS mkcert) + boucle fixed-timestep + squelette debug (FPS).
  ✅ *Valide si* : page ouvrable depuis l'iPhone via l'IP locale, FPS affichés, 60/120 Hz stables.

- ✅ **Étape 1 — Route nue** *(point de départ demandé par l'artiste)* — FAIT.
  Moteur pseudo-3D : sol qui défile vers le point de fuite, vitesse de base, déport latéral (au clavier d'abord), **aucun asset**. Grille rythmique de debug en surimpression.
  ✅ *Valide si* : la route défile fluide sur iPhone, la direction répond, la grille s'affiche.

- ✅ **Étape 2 — Audio & synchro** — FAIT (+ fondu in/out et volume réglable, ajoutés sur demande).
  Web Audio, unlock iOS, calage `BPM/offset`, le monde **pulse au tempo**, la grille tombe pile sur les temps.
  ✅ *Valide si* : sur mobile, la grille de debug colle au beat du morceau du début à la fin.

- ✅ **Étape 3 — Le cycliste** — FAIT.
  **3 silhouettes proposées, lisibles à 32 px** (d'après la photo : cheveux bouclés, maillot rugby vert/blanc, à vélo) → l'artiste choisit. Intégration du sprite, réglage du feel (sensibilité, agressivité des virages), **gyroscope + bouton saut**.
  ✅ *Valide si* : l'avatar est reconnaissable à 32 px et pilotable au gyro sur iPhone.
  → **Silhouette B ("penché en avant") choisie et intégrée.** Gyro + bouton saut + feel branchés. Il manque encore : test réel sur iPhone (gyro nécessite HTTPS, voir §2/mkcert), vies (arrivent avec le gameplay étape 5).

- ✅ **Étape 4 — Paris** — FAIT (sauf vérif iPhone réel).
  Décor : ciel coucher de soleil 22 h, façades haussmanniennes éclairées orange, défilement latéral, ambiance.
  ✅ *Valide si* : l'univers est lisible et tient 60 fps sur l'iPhone de référence.
  → Fait : caméra, ciel dégradé, façades haussmanniennes détaillées (pierre/toit/fenêtres/cheminées) d'après références artiste, sol + bâtiments fondus dans une brume chaude cohérente à distance. 120 fps stable en preview mobile. Reste : vérif fps + rendu réels sur iPhone (seul point non encore validé sur vrai appareil).

- ✅ **Étape 5 — Gameplay** — FAIT.
  3 vies, 3 obstacles, 5 bonus (spawn calé sur les beats), énergie/drain, score, montée de vitesse, game over + fin de morceau = fin de parcours.
  ✅ *Valide si* : une partie complète est jouable de bout en bout avec score cohérent.
  → Testé en preview : collision obstacle (vie perdue) et ramassage bonus (score+50) confirmés. Pas encore testé sur iPhone réel.

- ✅ **Étape 6 — Habillage & concours** — FAIT (l'habillage ; le classement effectif arrive avec le backend, étape 7).
  Menu, écran-titre, écran de fin, CTA "aller écouter" (→ linktr.ee/pmc.mp3), logique dates d'ouverture/fermeture (toujours jouable, classement conditionné à la fenêtre).
  ✅ *Valide si* : les états s'enchaînent proprement, le CTA fonctionne.
  → Fait : titre intégré à l'écran de démarrage existant (pas de nouvel état), CTA DOM (vrai lien) visible démarrage + fin / caché en jeu, statut concours affiché sur l'écran de fin. Testé en preview mobile, aucune erreur. Reste : validation iPhone réel (comme tout le reste), classement effectif à l'étape 7.

- **Étape 7 — Backend & partage**
  Supabase (table `scores`, POST/GET), meilleur score, classement. **Image story 1080×1920 pixel art arcade** + Web Share.
  ✅ *Valide si* : un score remonte au backend et l'image se partage depuis Safari iOS.

- **Étape 8 — Polish, perf & livraison**
  Tests croisés **Safari/Chrome/Firefox (iOS + Android)**, budget perf, safe-areas, repli gyro, déploiement public **avant le 11/09/2026**.
  ✅ *Valide si* : jouable publiquement, fluide, sur les 3 navigateurs cibles.

---

## 9. Décisions verrouillées & points mineurs

**Verrouillé :**
- Identifiant = **pseudo Instagram**, pas d'email.
- **Aucun anti-triche** : le backend garde tous les scores, vérif concours = screenshot.
- **Silhouette cycliste = variante B, "penché en avant"** (choisie à l'étape 3 parmi 3 propositions). Intégrée dans `src/player.js`.

**Points mineurs (non bloquants) :**
1. **Interprétation "énergie"** vs 3 vies (cf. note §4) — défaut retenu : énergie = allure, vies = mort.
2. **Police définitive** : Helvetica pour l'instant (fournie plus tard).
3. **iPhone/Android minimum** à supporter : viser large (iPhone SE 2e gén. comme plancher perf raisonnable) — à confirmer.

---

## 10. Prochaine action

⚠️ **Cette section a été réécrite : elle décrivait encore l'étape 7 comme à venir alors que Supabase
est branché et que le jeu est en ligne.** Le passage sur le gyroscope est lui aussi périmé — le
gyroscope a été retiré (décision verrouillée, voir `CLAUDE.md`), tout se joue au geste.

Étapes 0 à 7 faites, le jeu est déployé et joué par des testeurs externes
(https://pmc-la-ville-est-belle.netlify.app). Le bloc « ⚠️ À savoir » plus haut garde les pièges
d'environnement déjà rencontrés : serveur de dev qui meurt régulièrement, IP locale à revalider,
preview partagée qui peut montrer le mode debug par erreur. Ajouter à cette liste : **la preview
navigateur ne peut pas jouer le jeu** — créer l'`AudioContext` y fait figer l'onglet, donc tout ce qui
touche à l'audio, à la pause ou à l'écran de fin se vérifie en prod sur téléphone, pas en preview.

**Priorité n°1 — les 3 retours de la vague 3, non corrigés** (reprise de pause qui rembobine le
morceau, classement absent de l'écran de fin, cyclistes NPC traversés sans effet). Diagnostic complet
et pistes dans « État d'avancement » en tête de document ; le 3e demande une décision produit de
l'artiste avant tout code.

Ensuite, dans l'ordre le plus efficace :
1. **Image de partage 1080×1920** — c'est la moitié de l'étape 7 qui n'a jamais été faite (les scores Supabase, eux, tournent). Le style est déjà arrêté : écran de fin de borne d'arcade 80s, voir §11 point 15.
2. **Les 4 chantiers lourds restants** issus du 1er playtest externe (DA cycliste, son dégradé sur le dernier cœur, événement « ATTENTION », pont) — à prioriser avec l'artiste, plusieurs sont encore bloqués sur une référence visuelle jamais reçue.
3. **Étape 8 — Polish/perf/livraison** en dernier, une fois le reste posé.

Livrer un état « fait / reste » à la fin de chaque étape, comme d'habitude.

---

## 11. Liste des assets créa à produire (remplacer le dessin procédural)

Demandé explicitement après avoir buté sur les limites du dessin procédural (rectangles/cercles en
Canvas) pour la roue de vélo et les cheveux bouclés du personnage. Tout ce qui suit est dessiné en
code aujourd'hui (aucun fichier image chargé dans le jeu) ; une fois les fichiers fournis, il reste un
travail de code pour les charger (`Image()` + `drawImage()`) à la place des fonctions de dessin
actuelles (`player.js`, `entities.js`, `world.js`, `road.js`). Format par défaut sauf mention contraire :
**PNG transparent, pixel art "dur"** (pas d'antialiasing/dégradés doux, cohérent avec le reste du jeu).

**Tailles indiquées = celles exploitées par le code aujourd'hui.** Si un format différent est produit,
il suffit de me donner les dimensions exactes retenues pour que je recale les constantes correspondantes
(`SPRITE_W`/`SPRITE_H`, `ICON_SIZE`, etc.) — pas besoin de coller pile aux chiffres ci-dessous.

1. **Personnage (cycliste)** — sprite **26 × 34 px** par frame, vu de dos, penché en avant. **4 frames minimum** (cycle de pédalage : jambe gauche haute → transition → jambe droite haute → transition), toutes alignées sur le même ancrage (bas centré = contact roue/sol, sinon ça "saute" en boucle une fois animé). Sprite sheet horizontal ou fichiers séparés. Palette actuelle à respecter si on garde la cohérence : cheveux `#241609`, peau `#c98a5b`, maillot vert `#2f7a46` / blanc `#f0ead9`, pantalon `#22242b`, pneu `#0e0e11`. Doit couvrir ce qui coince en procédural : cheveux bouclés bien identifiables, vraie roue de vélo visible (pas cachée sous le pantalon). Bonus : une 5e pose dédiée au saut (aujourd'hui, juste le même sprite décalé vers le haut).
2. **Bonus — clémentine** — icône **20 × 20 px**.
3. **Bonus — clavier** — icône **20 × 20 px**.
4. **Bonus — sourire** — icône **20 × 20 px**.
5. **Bonus — collier de perles** — icône **20 × 20 px**.
6. **Bonus — étoile** — icône **20 × 20 px**.
7. **Obstacle — portière de voiture** — icône **20 × 20 px**.
8. **Obstacle — piéton** — icône **20 × 20 px**.
9. **Obstacle — trottinette** — icône **20 × 20 px**.
   *(2 à 9 : même grammaire pixel art que le personnage, simples et lisibles à petite taille — ils défilent vite, calés sur les beats. 32×32 possible si tu veux plus de détail.)*
10. **Bâtiments (façades)** — **pas de taille fixe** : chaque bâtiment est généré avec une largeur/hauteur aléatoire (4 à 8 unités-monde de large, 9 à 22 de haut, converties en pixels écran selon la distance/perspective — ça défile en continu, jamais une taille figée). Deux options : **(a)** une texture **répétable**, ex. une tuile "un étage de fenêtres" ~**32 × 48 px**, qu'on répète pour remplir n'importe quelle taille de bâtiment ; **(b)** un jeu de 3 à 5 bâtiments à **taille fixe** (ex. 64×160, 64×220, 96×140 px) si tu préfères composer à la main et accepter moins de variété.
11. **Route et trottoirs** — même logique que les bâtiments (défilement continu, pas de taille fixe) : une texture tileable, par exemple une tuile **~64 × 64 px** (chaussée + marquage au sol) répétée en continu.
12. **Logo/titre "LA VILLE EST BELLE"** — pour l'écran d'accueil (étape 6, pas encore fait) et l'image de partage (étape 7). Pas de taille précise à donner encore, ça dépendra de la maquette de l'écran-titre.
13. **Icônes HUD "vie"** (optionnel) — actuellement de simples cercles dessinés (3 vies) — suggestion **16 × 16 à 24 × 24 px** par icône si on veut remplacer par un vrai motif (cœur, roue, etc.).
14. **Boutons SAUT / Rejouer** (optionnel) — actuellement du CSS pur, pas d'image — habillage visuel façon arcade si tu veux, sinon on garde tel quel.
15. **Image de partage** (étape 7, plus tard) — canvas **1080 × 1920 px**, style écran de fin de borne d'arcade 80s : cadre de borne, logo, zone score/high score. À détailler le moment venu (§7 du plan).
