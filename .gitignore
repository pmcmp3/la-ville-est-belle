node_modules/
dist/
# build --base=./ pour le miroir GitHub Pages, voir ARCHITECTURE.md §9
dist-pages/
.DS_Store
.netlify/
.claude/settings.local.json

# Jamais servis ni committés (CLAUDE.md, ARCHITECTURE.md §9) — retirés de tout
# l'historique de gameplay-pause-voitures-etoiles le 17 août 2026 (65 Mo de
# WAV maître + le MP3 320k d'origine). Ignorés pour ne pas les réintroduire
# par un futur `git add -A`.
assets/la-ville-est-belle-MASTER.wav
assets/la-ville-est-belle-320k-original.mp3

# Dossier sans rapport avec le jeu (outil reels, autre projet) — présent
# incidemment dans ce répertoire, pas suivi ici.
reels/

# Assets FBX hors sujet (2,4 Mo), déjà exclus à la main des snapshots main/
# gh-pages (ARCHITECTURE.md §9). Ignorés ici pour la même raison que le WAV
# maître plus haut : c'est un `git add -A` distrait qui a mis 65 Mo de binaires
# dans l'historique public la dernière fois, et il a fallu réécrire tout le
# dépôt pour les en sortir.
3D assets/
